# Search-SDK call-site audit (D-96)

Generated by `scripts/audit_sdk_call_sites.py`. Do not edit by hand — CI regenerates this file and fails on drift.

**D-89:** the search SDK must execute on a live user request, not during data prep. A call site with no reaching endpoint is a FAIL.

| # | Call site | Method | Enclosing function | Reaching endpoint(s) | Verdict |
|---|---|---|---|---|---|
| 1 | `agent/job2.py:412` | `.search()` | `_real_search` | `POST /api/v1/research`<br>`POST /research` | PASS |
| 2 | `agent/live_checks.py:119` | `.search()` | `_search_excerpts` | `GET /compare`<br>`POST /api/v1/compare`<br>`POST /api/v1/spec`<br>`POST /spec` | PASS |
| 3 | `agent/parallel_client.py:78` | `.search()` | `search_for_disclosure` | `POST /api/v1/job1`<br>`POST /job1` | PASS |
| 4 | `agent/parallel_client.py:108` | `.extract()` | `extract_document` | `POST /api/v1/job1`<br>`POST /job1` | PASS |

## Dual reachability (disclosed, not a failure)

These call sites are reached by a request handler (satisfying D-89) and their defining module also exposes a command-line entry point, so the same code is reachable two ways:

- `agent/parallel_client.py:78` — also reachable via a command-line entry point in `agent/job1.py`
- `agent/parallel_client.py:108` — also reachable via a command-line entry point in `agent/job1.py`

**4 call site(s); 0 failing.**
