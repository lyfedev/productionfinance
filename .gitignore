# ── Secrets — NEVER commit. This repository is public per hackathon rules. ──
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
service-account*.json
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Node / frontend
node_modules/
.vite/
frontend/dist/

# Local data + caches
*.sqlite
*.sqlite3
*.db
.cache/
data/cache/

# Job 1 run persistence (agent/runs.py) — ephemeral, per-process working
# state, never the audit trail. `runs/job1/` (NOT ignored) is the separate,
# committed directory holding the one human-verified live run artifact.
var/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Additional key material shapes (D-30)
id_rsa*
id_ed25519*
*.ppk
LightsailDefaultKey*
.uv-cache/
.gstack/

# GSD ephemeral runtime state (session-scoped: live PID / dispatch sentinel)
.gsd/
.planning/milestone.lock
