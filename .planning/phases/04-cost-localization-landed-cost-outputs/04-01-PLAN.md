---
phase: 04-cost-localization-landed-cost-outputs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/figure.py
  - engine/figure_serialize.py
  - engine/pipeline.py
  - engine/cost_profile.py
  - engine/budget.py
  - engine/cost_localizer.py
  - engine/landed_cost.py
  - engine/city_profile_lookup.py
  - data/crew_tiers.yaml
  - data/cost_profiles/us-ny-new-york.yaml
  - data/union_rates/us-ny-crew.yaml
  - app/services/spec.py
  - app/routers/spec.py
  - app/templates/spec_result.html
  - tests/test_engine_figure_basis.py
  - tests/test_engine_cost_profile.py
  - tests/test_engine_budget.py
  - tests/test_engine_cost_localizer.py
  - tests/test_engine_landed_cost.py
  - tests/test_route_a_basis_walk.py
  - tests/test_app_spec_route.py
  - tests/test_engine_against_validation_pairs.py
  - tests/fixtures/cost_profiles/synthetic-minimal.yaml
autonomous: true
requirements: [COST-01, OUT-04]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor who describes a production and names New York gets back a real dollar total landed cost, not a stated boundary — Route A returns dollars (D-71)."
    - "The canonical budget is built once from the ProductionSpec and is byte-identical regardless of which candidate cities were named (COST-01)."
    - "Every cost-side Figure carries a `basis` of sourced, estimated or modelling_assumption, and the total inherits the weakest basis of its inputs (D-58/D-59)."
    - "`combined_basis` raises on a sequence carrying no basis, and never returns `sourced` by default (D-59's named landmine)."
    - "No Figure reachable from the Route A total carries `confidence: \"validated\"` — a modelled spend cannot produce a validated figure (D-63/D-71)."
    - "A validation-pair fixture never reaches `engine.budget`; disclosed spend still feeds `price_jurisdiction` directly (D-72)."
    - "The total names every cost category it has not yet priced and every category it will never price, as two separate first-class lists — never a $0 line (D-60)."
    - "Every budget line carries its ATL/BTL/Post chart-of-accounts tag at creation (OUT-04, D-77)."
    - "Route A and Route B stay visibly distinct now that both return a credit figure: Route A's response and page state, in plain words next to the number, that its qualified spend is MODELLED from the described production, not a disclosed government figure (D-73/D-32)."
    - "A visitor-supplied city string can never select a filesystem path — cost profiles resolve through an explicit committed allow-list only."
  artifacts:
    - engine/cost_profile.py
    - engine/budget.py
    - engine/cost_localizer.py
    - engine/landed_cost.py
    - engine/city_profile_lookup.py
    - data/cost_profiles/us-ny-new-york.yaml
    - data/union_rates/us-ny-crew.yaml
    - tests/test_route_a_basis_walk.py
    - tests/test_engine_figure_basis.py
  key_links:
    - "engine/figure.py::basis -> engine/figure_serialize.py::figure_to_dict -> the JSON contract Phases 6/8/10 render against"
    - "app/services/spec.py -> engine.budget.build_canonical_budget -> engine.cost_localizer.localize -> engine.landed_cost.aggregate -> engine.pipeline.price_jurisdiction (the D-36 seam closing)"
    - "engine.pipeline.price_programme spend_confidence -> every downstream Figure's confidence tier (the mechanism that makes D-63 true rather than aspirational)"
  prohibitions:
    - "No AWS AI endpoint is called or imported — Textract, Bedrock, Comprehend, Rekognition, Transcribe, Polly, Translate, Kendra, SageMaker, Amazon Q. AI services are Google Cloud plus Parallel only."
    - "No new runtime dependency is added to pyproject.toml or uv.lock in this phase (D-57)."
    - "No runtime network call is made by any code path this phase adds (D-57)."
    - "No `.quantize()` call site outside engine/rounding.py::quantize_money."
    - "No YAML is loaded with anything other than `yaml.safe_load`."
    - "No third-party money library — stdlib decimal.Decimal only."
    - "No unmodelled incentive is rendered as a zero figure (D-56)."
    - "No validation-pair fixture is routed through the budget model (D-72)."
    - "No cost figure carries `basis: sourced` without a primary government or union document archived under sources/ with a MANIFEST entry."
    - "No Figure reachable from a Route A total carries `confidence: \"validated\"` (D-63)."
    - "No unquoted numeric value in any new YAML data file (RD-01)."
    - "No sleep, spinner or progress bar standing in for work that is not happening."
---

<objective>
Close the D-36 seam with a production-quality vertical slice: a described production becomes one canonical budget (COST-01), that budget is localized against New York's committed cost profile, aggregated into a landed cost, and returned as real dollars through `/api/v1/spec` — with the new `basis` provenance axis (D-58) carried on every cost figure and enforced end to end by a basis-walk gate (D-63).

Purpose: prove the whole cost architecture on one path before any category is broadened. Every later plan in this phase widens this slice horizontally; none of them changes its shape.
Output: the `basis` axis on `Figure`, five new engine modules, New York's cost profile, Route A returning dollars, and the three honesty gates (D-59 empty-sequence refusal, D-63 basis walk, D-72 validation-pair guard).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-CONTEXT.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-RESEARCH.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-PATTERNS.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-VALIDATION.md
@jurisdictions/SCOPE-FREEZE.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "price New York from a described production" — one cost line, every layer</name>
  <files>engine/figure.py, engine/figure_serialize.py, engine/pipeline.py, engine/cost_profile.py, engine/budget.py, engine/cost_localizer.py, engine/landed_cost.py, engine/city_profile_lookup.py, data/cost_profiles/us-ny-new-york.yaml, data/union_rates/us-ny-crew.yaml, app/services/spec.py, app/routers/spec.py, app/templates/spec_result.html, tests/test_engine_figure_basis.py, tests/test_app_spec_route.py</files>
  <read_first>
    - engine/figure.py (the frozen kw_only dataclass, `combined_confidence`'s empty-sequence default — the D-59 landmine, and the RD-02 two-vocabularies docstring)
    - engine/figure_serialize.py (the only Figure-to-JSON path)
    - engine/rounding.py (the single pinned quantize call site)
    - engine/spec.py (ProductionSpec, CrewHeadcount, resolve_crew_tier, CREW_TIERS_PATH, the local StrictModel mirror, the yaml.safe_load-only loader)
    - engine/qualifying_base.py lines 40-120 and 260-333 (SpendBreakdown, from_total, compute_qualifying_base's signature and the D-02 boundary docstring)
    - engine/pipeline.py lines 55-120 and 209-277 (price_programme's confidence derivation, price_jurisdiction's summation and total Figure construction)
    - engine/net_cash.py lines 40-64 and 154-186 (NetCashResult, transferable's refuse-rather-than-invent shape)
    - engine/models.py lines 94-155 and 379-end (StrictModel, Source, Jurisdiction, load_ruleset)
    - app/services/spec.py (full file — the docstring's forbidding claim, SPEND_NOT_DERIVED, handle_spec_submission's ordered sequence, REFUSAL_REASON)
    - app/services/_paths.py (REPO_ROOT, RULESET_PATH_BY_JURISDICTION)
    - app/services/city_lookup.py (CITY_ALIASES — the explicit allow-list convention cost-profile resolution must copy)
    - app/routers/spec.py (_spec_result_to_json, the three route handlers)
    - app/templates/spec_result.html (renders result.spend_not_derived today)
    - data/crew_tiers.yaml (the basis / provenance_note header convention D-58's vocabulary comes from)
    - jurisdictions/us-ny.yaml (RD-01 quoted-string convention; `status: curated_validated`, `currency: USD`, `mechanism: refundable`)
    - .planning/phases/04-cost-localization-landed-cost-outputs/04-PATTERNS.md (the per-file analog assignments)
  </read_first>
  <action>
Wire ONE cost line through every layer, end to end, and stop there. No second category, no second city.

**1. The `basis` axis on `Figure` (D-58).** In `engine/figure.py` add `Basis = Literal["sourced", "estimated", "modelling_assumption"]` and a field `basis: Basis | None = None` declared AFTER every existing required field and before or beside `figure_id`'s `default_factory` (it must default to `None`, because every existing construction site in `engine/credit.py`, `engine/net_cash.py`, `engine/qualifying_base.py` and `engine/pipeline.py` omits it). Validate it in `__post_init__` against the closed tuple the same way `confidence` already is. Add `combined_basis(inputs: Sequence[Figure]) -> Basis` mirroring `combined_confidence`'s weakest-wins shape with one inversion: on a sequence containing no basis-carrying input it raises `ValueError` naming D-59, and it never returns `sourced` as a fallback. Order for weakest-wins is `modelling_assumption` weakest, then `estimated`, then `sourced` strongest. Export `Basis` and `combined_basis` from `__all__`. In `engine/figure_serialize.py` add a single `"basis": figure.basis` key to `figure_to_dict`, positioned next to `"confidence"`; it serializes as `null` for every existing incentive-side figure.

**2. Weakest-wins confidence at the modelled-spend seam (`engine/pipeline.py`).** `price_programme` currently derives `confidence` from `jurisdiction.status` alone, so New York's curated status would stamp `validated` onto figures computed from a MODELLED spend — which would make D-63 unachievable and D-71's "its qualified spend is never validated" false. Add a keyword-only `spend_confidence: Confidence = "validated"` to both `price_programme` and `price_jurisdiction`, threaded through, and change the derivation so the result is `validated` only when `jurisdiction.status == "curated_validated"` AND `spend_confidence == "validated"`, otherwise `researched`. The default preserves every existing caller (Route B, the validation-pair suite) byte-for-byte. While here, add a keyword-only `spend_breakdown: SpendBreakdown | None = None` to both functions: when supplied it is used in place of `SpendBreakdown.from_total(qualified_spend)`, closing the D-02 note in `engine/qualifying_base.py`'s docstring that Phase 4 finally feeds a real localized breakdown. Both parameters are additive with behaviour-preserving defaults — this is a deliberate, reasoned deviation from 04-RESEARCH.md's "pipeline.py UNCHANGED" note, recorded in the SUMMARY.

**3. `engine/cost_profile.py` — the `CityCostProfile` schema and loader.** Mirror `engine/spec.py:39-46,62-63,158-176`: a local two-line `StrictModel` with `model_config = ConfigDict(extra="forbid")` (not imported from `engine.models`), a module-anchored `COST_PROFILES_DIR = Path(__file__).resolve().parents[1] / "data" / "cost_profiles"`, and `load_cost_profile(path)` reading with `yaml.safe_load` then `model_validate`. `CityCostProfile` fields for the tracer: `city_id`, `city_label`, `jurisdiction_id: str | None` (D-53 — a cost profile needs no rule file), `currency`, `provenance_note`, and `cost_lines: list[CostLine]`. `CostLine` fields: `line_id`, `label`, `category` (a closed Literal over the canonical category vocabulary), `account` (a closed Literal `"ATL" | "BTL" | "POST"` — OUT-04/D-77), `spend_class` (a closed Literal `"local_labour" | "local_non_labour" | "non_local"` — this is how the SpendBreakdown is derived from declared data rather than a jurisdiction branch, JUR-05), `unit_rate` (quoted string, RD-01), `rate_unit`, `basis` (the D-58 vocabulary), `source_url: str | None`, `date_checked: str | None`, `method_note` (required whenever `basis` is not `sourced` — the disclosed method, PITFALLS E1/E5). Add a model validator rejecting `basis: "sourced"` on a line whose `source_url` is null.

**4. `engine/city_profile_lookup.py` — the path-safety boundary.** A committed `COST_PROFILE_BY_CITY: dict[str, str]` mapping a casefolded city string to a profile stem, and `resolve_city_to_profile_stem(raw_city) -> str | None`, copying `app/services/city_lookup.py`'s discipline exactly: `strip().casefold()`, an explicit allow-list hit or `None`, no fuzzy match, no substring scan. A visitor's string is NEVER interpolated into a `Path`; only a stem drawn from this committed dict is. Seed it with New York's aliases matching the existing `CITY_ALIASES` New York entries.

**5. `engine/budget.py` — stage [1], `build_canonical_budget`.** Mirror `engine/qualifying_base.py`'s shape: a frozen `CanonicalBudget` dataclass (`line_quantities: dict[str, Decimal]`, `accounts: dict[str, str]`, `shoot_days: Decimal`, `crew_headcount: CrewHeadcount`) plus `build_canonical_budget(spec: ProductionSpec, crew_headcount: CrewHeadcount) -> CanonicalBudget`. It is a pure function of the spec and `data/crew_tiers.yaml` and takes NO city argument — that is COST-01 made structural. For the tracer it emits one quantity line: crew labour days = `(shoot_days_stage + shoot_days_location) * crew headcount`, using the headcount low bound with the choice stated in a derivation line. Every emitted line carries its `account` tag. Use `Decimal` throughout and `quantize_money` for any money-shaped rounding.

**6. `engine/cost_localizer.py` — stage [2], `localize`.** `localize(budget: CanonicalBudget, profile: CityCostProfile) -> LocalizedBudget`, where `LocalizedBudget` is a frozen dataclass carrying `city_id`, `jurisdiction_id`, `currency`, `lines: tuple[Figure, ...]`, and a `spend_breakdown: SpendBreakdown` derived from the lines' declared `spend_class`. It dispatches only on data declared in `CityCostProfile` and never on a jurisdiction id string (D-53/JUR-05). Each line Figure gets `value = quantize_money(quantity * Decimal(unit_rate))`, `unit = profile.currency`, a derivation naming the quantity, the rate, the rate unit and the profile's `source_url`, `basis` copied from the profile line, and `confidence="researched"`. Add `_find_line_by_label` performing lookup by label, never by position, mirroring `engine/net_cash.py`'s by-label discipline.

**7. `engine/landed_cost.py` — stage [6], `aggregate`.** Declare `COST_CATEGORIES` (the canonical tuple: `labour`, `fringe`, `housing`, `per_diem`, `flights`, `stages`, `equipment`, `permits`, `locations`, `trucking`) and `PERMANENT_EXCLUSIONS` (D-60: overtime, turnaround penalties, meal penalties, kit fees, non-union local differentials, negotiated hotel rates). `aggregate(localized, net_cash_figure_or_none)` returns a frozen `LandedCost` with `cost_total: Figure`, `total_landed_cost: Figure`, `not_priced: tuple[str, ...]` (every entry of `COST_CATEGORIES` with no line in this profile) and `permanent_exclusions: tuple[str, ...]`. Sum by mirroring `engine/pipeline.py:238-256` exactly — carry every line as an `inputs` entry, quantize the total once with `quantize_money`, set `confidence=combined_confidence(inputs)` and `basis=combined_basis(inputs)`. An unpriced category is NEVER a zero line; it is a named entry in `not_priced`.

**8. Route A returns dollars (D-71) — `app/services/spec.py`.** Delete the `SPEND_NOT_DERIVED` constant, its `SpecResult.spend_not_derived` field, its `__all__` entry, and the module docstring paragraph forbidding `engine.pipeline` / `engine.qualifying_base` imports; replace that paragraph with one stating the reverse and naming D-71 and the D-63 gate that now guards the seam. Keep the budget-refusal branch, `REFUSAL_REASON`, and the ordered sequence in `handle_spec_submission` exactly as they are — the refusal still runs BEFORE any `ProductionSpec` is constructed. After city assessment, for each candidate city that resolves to a committed cost profile stem: load the profile, build the canonical budget once for the whole submission (not once per city), localize, and — when `RULESET_PATH_BY_JURISDICTION` has an entry for that profile's `jurisdiction_id` — call `price_jurisdiction(ruleset, qualified_spend, spend_breakdown=..., spend_confidence="researched")` and aggregate. Add a `CityCost` dataclass to `SpecResult` carrying `city_id`, `cost_total`, `total_landed_cost`, `not_priced`, `permanent_exclusions`, and `incentive_state` (`"modelled"` or `"not_modelled"` with a plain-words reason). A city with no committed cost profile keeps its Phase 3 behaviour and produces no figures.

**9. The JSON and HTML ends, and the D-73 distinction.** In `app/routers/spec.py` extend `_spec_result_to_json` with a `city_costs` list, serializing every Figure through `figure_to_dict` (never `dataclasses.asdict`). In `app/templates/spec_result.html` replace the `result.spend_not_derived` block with a section rendering, per city, the total landed cost, its basis, the `not_priced` list and the `permanent_exclusions` list. Keep the template plain — Phase 6 owns the interface.

Both routes now return a credit figure, so the ONLY thing distinguishing them is where the qualified spend came from — and D-73 says that difference is exactly what the page must make visible. Add a `spend_origin` field to `SpecResult` holding a plain sentence stating that this qualified spend is modelled from the production the visitor described and is not a figure any government has disclosed, and render it adjacent to the number rather than in a footer. Route B's page is unchanged and keeps saying its spend is a disclosed figure; do not touch `app/services/validate.py`.

**10. Data.** `data/union_rates/us-ny-crew.yaml` holds the one crew day-rate row the tracer prices, following `data/crew_tiers.yaml`'s header shape (a leading comment saying what the file is and that it must never declare a `confidence` or `status` key, a top-level `basis`, a `provenance_note`, every numeric value a quoted string). Tag it `sourced` ONLY if you fetch and archive the union's own published rate card under `sources/unions/` with a `sources/MANIFEST.yaml` entry carrying its sha256; otherwise tag it `estimated` with `method_note` naming the actual non-primary source you used. Do not stall the tracer on document acquisition — the basis field is what tells the truth. `data/cost_profiles/us-ny-new-york.yaml` declares New York's identity, `jurisdiction_id: us-ny`, `currency: "USD"`, and the single `labour` cost line referencing that rate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine_figure_basis.py tests/test_app_spec_route.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `engine/figure.py` contains `Basis = Literal["sourced", "estimated", "modelling_assumption"]` and `def combined_basis(`
    - `combined_basis(())` raises `ValueError`; `combined_basis` over a mixed sequence containing one `modelling_assumption` returns `"modelling_assumption"`
    - `figure_to_dict` output contains a `"basis"` key at every level of the recursive tree
    - `engine/pipeline.py` contains `spend_confidence` and `spend_breakdown` as keyword-only parameters with behaviour-preserving defaults; the pre-existing full suite still passes unchanged
    - `POST /api/v1/spec` with a New York candidate city returns HTTP 200 whose body contains a `city_costs` entry with a non-zero `total_landed_cost.value` string parseable as a `Decimal`
    - The same response contains no `spend_not_derived` key, and `grep -rn "SPEND_NOT_DERIVED" app/ engine/` returns no hits
    - The response and the rendered page both carry a `spend_origin` sentence stating the qualified spend is modelled from the described production and is not a disclosed government figure (D-73), and `app/services/validate.py` is unchanged
    - Every Figure in that response carries a non-null `basis`, and the top-level `total_landed_cost` basis equals the weakest basis among its inputs
    - `city_costs[0].not_priced` names every category in `COST_CATEGORIES` that has no line in the profile, and no cost line in the response has `value` equal to `"0"` for an unpriced category
    - `engine/cost_localizer.py` contains no jurisdiction-id string literal (no `"us-ny"`, no `us-ca`) — dispatch is on declared profile data only
    - A visitor city string is never used to build a path: `engine/city_profile_lookup.py` is the only module that maps a city string to a profile stem, and it does so through a committed dict
  </acceptance_criteria>
  <reversibility rating="costly">Adding `basis` to `Figure` and to `figure_to_dict` fixes the JSON contract Phases 6, 8 and 10 render against; changing its vocabulary later is a coordinated change across four phases.</reversibility>
  <done>A described production plus "New York, NY" returns a real, cited, basis-tagged dollar total landed cost over HTTP, and the committed suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: The three honesty gates — basis walk, empty-sequence refusal, validation-pair guard</name>
  <files>tests/test_route_a_basis_walk.py, tests/test_engine_figure_basis.py, tests/test_app_spec_route.py, tests/test_engine_against_validation_pairs.py, tests/test_engine_cost_profile.py, tests/fixtures/cost_profiles/synthetic-minimal.yaml</files>
  <read_first>
    - tests/test_app_spec_route.py lines 220-270 (the import-boundary guard `test_route_a_service_never_imports_the_pricing_path` and `test_spend_not_derived_statement_present` — both reversed by D-71)
    - tests/test_engine_against_validation_pairs.py (the fixture-driven sorted-glob, fail-loud-on-empty discipline and its RD-03 docstring)
    - tests/test_source_truth.py (the structural CI-gate-as-pytest-assertion pattern)
    - tests/test_engine_models.py (the schema-load test shape `tests/test_engine_cost_profile.py` mirrors)
    - engine/figure.py and engine/figure_serialize.py (as modified by Task 1)
    - app/services/spec.py (as modified by Task 1)
  </read_first>
  <action>
Replace the guard D-71 legitimately reverses; do not simply delete it.

**`tests/test_route_a_basis_walk.py` (D-63).** Submit a `SpecFormSubmission` naming New York through `handle_spec_submission`, serialize every returned Figure through `figure_to_dict`, and walk the full recursive `inputs` DAG. Assert: (a) no node anywhere in the tree has `confidence == "validated"`; (b) every node reachable from a cost-side total has a non-null `basis`; (c) the walk actually visited more than a handful of nodes — assert a minimum visited-node count so a future refactor that flattens or truncates the tree makes this test red rather than vacuously green. Include a non-vacuity proof in the test module docstring recording that you temporarily forced `spend_confidence="validated"` at the call site, watched this test go red, and reverted.

**`tests/test_engine_figure_basis.py` (D-59).** Cover: `combined_basis` on an empty sequence raises `ValueError`; on a sequence whose members all have `basis=None` raises; weakest-wins across each pair of the three values; and an explicit regression asserting `combined_basis` does NOT mirror `combined_confidence`'s empty-sequence default — the exact one-line bug D-59 names in advance.

**`tests/test_engine_against_validation_pairs.py` (D-72).** Add a guard asserting no validation-pair fixture is ever routed through the budget model: assert that `engine.budget` is absent from `sys.modules`-independent import graph of this test module's pricing path, and — the substantive half — assert that every pair's asserted figure is still produced by feeding the fixture's DISCLOSED `qualified_spend` straight into `price_jurisdiction`, with no `ProductionSpec` constructed anywhere in the module. Add a fixture-field guard: no validation-pair YAML may carry any `ProductionSpec` input-vector field (`shoot_days_stage`, `crew_size`, `crew_tier`, `principal_cast_count`, `candidate_cities`), so a future contributor cannot quietly add one. Write the reason into the test's docstring in one sentence: disclosures publish qualified spend and the award, never the production's input vector, so a comparison routed through a modelled input vector would be measuring a fabrication.

**`tests/test_app_spec_route.py`.** Delete `test_spend_not_derived_statement_present` and `test_route_a_service_never_imports_the_pricing_path`, and replace the second with a test asserting the reverse is now deliberate: `app.services.spec` DOES reach the pricing path, AND the D-63 basis walk over its output holds. Update `test_route_a_derives_no_money`: Route A now legitimately derives money, so the forbidden-money-key assertion must be replaced by an assertion that every money value it does derive is a `Figure` dict carrying `basis`, `source_url`, `date_checked` and `confidence` — never a bare number.

**`tests/test_engine_cost_profile.py` + `tests/fixtures/cost_profiles/synthetic-minimal.yaml`.** A schema-load test mirroring `tests/test_engine_models.py`: a valid synthetic profile loads; an unknown field is rejected by `extra="forbid"`; a line with `basis: "sourced"` and a null `source_url` is rejected; a line whose `basis` is not `sourced` and whose `method_note` is missing is rejected; and the fixture directory is loaded by sorted glob with a fail-loud assertion if the glob is empty.
  </action>
  <verify>
    <automated>uv run pytest tests/test_route_a_basis_walk.py tests/test_engine_figure_basis.py tests/test_engine_cost_profile.py tests/test_engine_against_validation_pairs.py tests/test_app_spec_route.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_route_a_basis_walk.py -q` exits 0 and the test asserts a minimum visited-node count greater than 5
    - `uv run pytest tests/test_engine_figure_basis.py -q` exits 0 and includes a test whose name contains `empty` asserting `ValueError`
    - `grep -rn "test_spend_not_derived_statement_present\|test_route_a_service_never_imports_the_pricing_path" tests/` returns no hits
    - `tests/test_engine_against_validation_pairs.py` contains no `ProductionSpec` construction and no `engine.budget` import
    - The full suite passes: `uv run --frozen pytest tests/ -q` exits 0
    - The basis-walk test module docstring records the temporary-forcing non-vacuity proof with its observed red result
  </acceptance_criteria>
  <done>All three honesty gates exist as pytest tests inside the existing CI `tests` job, each proven non-vacuous, and the retired Phase 3 guard is replaced rather than dropped.</done>
</task>

<task type="auto">
  <name>Task 3: Department ratios and chart-of-accounts tags (D-38 deferred columns, OUT-04/D-77)</name>
  <files>data/crew_tiers.yaml, engine/budget.py, tests/test_engine_budget.py, tests/test_engine_landed_cost.py</files>
  <read_first>
    - data/crew_tiers.yaml (the existing header, `basis: modelling_assumption`, `provenance_note`, quoted-string tiers)
    - engine/spec.py lines 138-176 (`CrewHeadcount`, `UnknownCrewTierError`, `resolve_crew_tier`'s never-fall-back-to-a-default contract)
    - engine/budget.py (as created in Task 1)
    - engine/landed_cost.py (as created in Task 1)
    - .planning/research/PITFALLS.md Part E (E5 — every cost line carries the same confidence discipline as an incentive figure)
    - 04-RESEARCH.md Assumptions Log row A3 (ATL/BTL/Post department-ratio percentages are industry-blog aggregates, never a primary document)
  </read_first>
  <action>
Extend `data/crew_tiers.yaml` with the department-ratio columns D-38 deferred to this phase, and make `build_canonical_budget` emit one quantity line per department, each carrying its chart-of-accounts tag.

Add a top-level `departments:` block to `data/crew_tiers.yaml` naming the departments the budget models — at minimum production, camera, grip and electric, art, wardrobe, hair and makeup, sound, transportation, locations, post — each with an `account` of `ATL`, `BTL` or `POST`, and a per-tier `crew_share` quoted-string fraction of the resolved headcount. Every share is `basis: modelling_assumption` and the file's existing `provenance_note` must be extended, not replaced, to state that the department shares are industry-commentary aggregates rather than a primary accounting-standard document (research row A3). The shares must sum to `"1"` exactly per tier, asserted by a test — a silent residue would be an unpriced department wearing no label. The file must still never declare a `confidence` or `status` key.

Extend `resolve_crew_tier`'s neighbourhood with a `resolve_departments(tier) -> tuple[DepartmentShare, ...]` reader in `engine/spec.py` or `engine/budget.py` (pick one and say which in the SUMMARY), loading with `yaml.safe_load` from the same module-anchored `CREW_TIERS_PATH`, raising a named error for a tier absent from the table rather than returning a default — the identical contract `UnknownCrewTierError` already holds.

In `engine/budget.py`, replace the tracer's single crew-labour quantity line with one quantity line per department: department headcount = crew headcount times that department's `crew_share`, labour days = department headcount times total shoot days. Each emitted line carries `account` from the table and `basis="modelling_assumption"`. `CanonicalBudget.total_quantity` must equal the tracer's previous single-line quantity to within the rounding contract — assert that in a test, so the split is proven to be a decomposition rather than a new number.

OUT-04 isolation (the ROADMAP cut line): the `account` tag is a single additive field that nothing else in Phase 4 reads. `engine/landed_cost.py` must NOT group, subtotal or render by account — that view is Phase 11's (D-77). Add a test asserting every budget line and every localized cost line carries a non-null `account` drawn from the closed three-value set, and a second test asserting the aggregator's output is byte-identical whether or not account tags are present, proving the tag can be dropped without unpicking the rest.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine_budget.py tests/test_engine_landed_cost.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `data/crew_tiers.yaml` contains a `departments:` block and every numeric value in it is a quoted string
    - `grep -c 'confidence:\|status:' data/crew_tiers.yaml` returns 0 when comments are excluded (`grep -v '^#' data/crew_tiers.yaml | grep -c 'confidence:\|status:'` returns 0)
    - A test asserts each tier's department `crew_share` values sum to exactly `Decimal("1")`
    - A test asserts an unknown tier raises rather than returning a default department set
    - A test asserts every `CanonicalBudget` line carries an `account` in `{"ATL", "BTL", "POST"}`
    - A test asserts `engine.landed_cost.aggregate` output is unchanged when account tags are stripped — the OUT-04 droppability proof
    - `uv run --frozen pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>The canonical budget decomposes into per-department lines, each account-tagged, with the tag proven additive and droppable, and every share labelled a modelling assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → Route A (`POST /spec`, `POST /api/v1/spec`) | Untrusted spec input crosses here; free-text city names are the only string that reaches data selection |
| committed YAML under `data/` → engine loaders | Repo-controlled data crosses into typed models; a hand-edit is the realistic tamper vector |
| computed `Figure` tree → JSON response | Provenance claims cross to the public page; a false tier here is a product-level lie |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `engine/city_profile_lookup.py`, cost-profile path construction | high | mitigate | Visitor strings resolve through a committed `COST_PROFILE_BY_CITY` allow-list dict; only a stem drawn from that dict is joined to `COST_PROFILES_DIR`. No f-string interpolation of user input into a `Path` anywhere. Task 1 acceptance criterion asserts this. |
| T-04-02 | Tampering | every new YAML reader | medium | mitigate | `yaml.safe_load` exclusively, matching `engine/spec.py:158`; never `yaml.load` or `yaml.unsafe_load`. |
| T-04-03 | Denial of Service | `engine/cost_profile.py` schema load | medium | mitigate | Pydantic `extra="forbid"` plus `model_validate` at load time so a malformed committed file fails in CI rather than mid-request; covered by `tests/test_engine_cost_profile.py`. |
| T-04-04 | Repudiation | `combined_basis`, `price_programme` confidence derivation | high | mitigate | `combined_basis` raises on an empty sequence (never defaults to the strongest tier); `spend_confidence` weakest-wins forces `researched` on the modelled-spend path; `tests/test_route_a_basis_walk.py` walks the whole DAG and fails on any `validated` node. |
| T-04-05 | Information Disclosure | `figure_to_dict` derivation strings | low | mitigate | Derivation lines and refusal reasons carry no absolute filesystem paths — only repo-relative identifiers and public source URLs. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs zero packages (D-57); the existing `lockfile-scan` CI job blocks any addition, and `pyproject.toml`/`uv.lock` are not in `files_modified`. No `[ASSUMED]` or `[SUS]` package exists to gate. |
</threat_model>

## Artifacts this phase produces

New symbols and files created by this plan (excluded from drift verification — they do not exist yet):

- `engine/figure.py`: `Basis`, `combined_basis`, `Figure.basis`
- `engine/pipeline.py`: `price_programme(..., spend_confidence=, spend_breakdown=)`, `price_jurisdiction(..., spend_confidence=, spend_breakdown=)`
- `engine/cost_profile.py`: `StrictModel`, `COST_PROFILES_DIR`, `CostLine`, `CityCostProfile`, `load_cost_profile`
- `engine/city_profile_lookup.py`: `COST_PROFILE_BY_CITY`, `resolve_city_to_profile_stem`
- `engine/budget.py`: `CanonicalBudget`, `build_canonical_budget`, `DepartmentShare`, `resolve_departments`
- `engine/cost_localizer.py`: `LocalizedBudget`, `localize`, `_find_line_by_label`
- `engine/landed_cost.py`: `COST_CATEGORIES`, `PERMANENT_EXCLUSIONS`, `LandedCost`, `aggregate`
- `app/services/spec.py`: `CityCost`, `SpecResult.city_costs`
- `data/cost_profiles/us-ny-new-york.yaml`, `data/union_rates/us-ny-crew.yaml`, `data/crew_tiers.yaml` `departments:` block
- `tests/test_engine_figure_basis.py`, `tests/test_engine_cost_profile.py`, `tests/test_engine_budget.py`, `tests/test_engine_cost_localizer.py`, `tests/test_engine_landed_cost.py`, `tests/test_route_a_basis_walk.py`, `tests/fixtures/cost_profiles/synthetic-minimal.yaml`

Removed by this plan: `app/services/spec.py::SPEND_NOT_DERIVED`, `SpecResult.spend_not_derived`, `tests/test_app_spec_route.py::test_spend_not_derived_statement_present`, `tests/test_app_spec_route.py::test_route_a_service_never_imports_the_pricing_path`.

## Flagged Assumptions — unresolved edge probes

No SPEC exists for this phase, so the deterministic edge probe ran instead. These rows came back `unclassified` and are recorded here as open assumptions rather than silently dropped. They are NOT auto-resolved and NOT auto-dismissed.

| Requirement | Probe | Status |
|---|---|---|
| COST-01 | unclassified — review manually | unresolved — carried into `/gsd-verify-work` as an explicit assumption |
| OUT-04 | unclassified — review manually | unresolved — carried into `/gsd-verify-work` as an explicit assumption |

## Deviations recorded at plan time

- **04-RESEARCH.md says `engine/pipeline.py` is UNCHANGED.** Task 1 adds two keyword-only parameters with behaviour-preserving defaults. Without `spend_confidence`, New York's `curated_validated` status stamps `confidence: "validated"` onto figures computed from a modelled spend, which makes D-63 unachievable and D-71's "its qualified spend is never validated" false. This is a real finding, not a convenience; it is recorded here, must be recorded in the SUMMARY, and the pre-existing suite must stay green to prove the defaults preserve Route B.
- **Archiving policy for non-`sourced` anchors.** `sources/MANIFEST.yaml` archival is required for every figure tagged `basis: sourced`. A figure tagged `estimated` records its `source_url` and `method_note` in the data file and does not require a byte-archived document. This keeps D-08's audit trail attached to the claims that actually assert a citation.

<verification>
- `uv run --frozen pytest tests/ -q` exits 0 (the exact command the CI `tests` job runs)
- `POST /api/v1/spec` with a New York candidate returns a `city_costs` entry carrying a non-zero `total_landed_cost`
- No node in that response's Figure DAG has `confidence: "validated"`
- `grep -rn "SPEND_NOT_DERIVED" app/ engine/ tests/` returns no hits
- `grep -rn "\.quantize(" engine/ | grep -v "engine/rounding.py"` returns no hits
- `grep -rn "yaml.load\|yaml.unsafe_load" engine/ app/` returns no hits
- `git diff --stat pyproject.toml uv.lock` is empty — no dependency was added
</verification>

<success_criteria>
- A described production plus "New York, NY" returns a real, cited, basis-tagged dollar landed cost over HTTP
- The canonical budget is provably city-independent (COST-01)
- Every budget line carries its ATL/BTL/Post tag, proven additive and droppable (OUT-04)
- D-59, D-63 and D-72 exist as non-vacuous pytest tests inside the existing CI job
- The phase's remaining plans can widen this slice by adding cost categories without changing its shape
</success_criteria>

<output>
Create `.planning/phases/04-cost-localization-landed-cost-outputs/04-01-SUMMARY.md` when done. Record: the `spend_confidence` deviation and why it was necessary; the basis tier actually assigned to the New York crew rate row and whether a primary document was archived; the exact `total_landed_cost` Decimal the tracer produces for the fixed test spec (later plans will move it, and the delta must be explainable).
</output>
</content>
