---
phase: 04-cost-localization-landed-cost-outputs
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - data/union_rates/us-ny-crew.yaml
  - data/union_rates/iatse.yaml
  - data/union_rates/sag-aftra.yaml
  - data/union_rates/dga.yaml
  - data/union_rates/wga.yaml
  - data/union_rates/fringe_schedules.yaml
  - data/cost_profiles/us-ny-new-york.yaml
  - data/cost_profiles/us-ca-los-angeles.yaml
  - sources/MANIFEST.yaml
  - engine/cost_profile.py
  - engine/cost_localizer.py
  - engine/union_rates.py
  - tests/test_engine_union_rates.py
  - tests/test_engine_cost_localizer.py
  - .planning/WINDOWS.md
autonomous: true
requirements: [COST-02, COST-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "This plan builds Los Angeles, the second of the three floor cities of the committed city set (D-54: the floor is New York, Los Angeles and London) — the canonical producer comparison, carrying a full cost profile and no jurisdiction rule file."
    - "Labour is priced from published union rate-card rows (IATSE, SAG-AFTRA, DGA, WGA) held as committed dated snapshots, never from an invented blended day rate (COST-02)."
    - "Fringe and payroll burden are a separate named Figure attached to the labour line, never folded into the wage figure (COST-03, D-62)."
    - "A fringe percentage published by a union's own document is `basis: sourced`; a blanket multiplier used where no union figure exists is `basis: estimated` and carries its method and its actual non-primary source on the figure (PITFALLS E1)."
    - "A shoot start date falling exactly on a union rate row's `effective_from` selects that row; one day earlier selects the prior row; a date covered by no row raises rather than falling back to the nearest row (closed-closed dated-range convention, WR-03)."
    - "Labour cost is computed as headcount times days times rate in Decimal and quantized exactly once through `engine.rounding.quantize_money` with ROUND_HALF_UP; a product landing on a half-dollar boundary rounds up, and no intermediate `.quantize()` call exists."
    - "Los Angeles has a committed cost profile with no jurisdiction rule file, proving cost localization is independent of the incentive side (D-53)."
  artifacts:
    - data/union_rates/iatse.yaml
    - data/union_rates/sag-aftra.yaml
    - data/union_rates/dga.yaml
    - data/union_rates/wga.yaml
    - data/union_rates/fringe_schedules.yaml
    - data/cost_profiles/us-ca-los-angeles.yaml
    - engine/union_rates.py
    - tests/test_engine_union_rates.py
  key_links:
    - "data/union_rates/*.yaml -> engine/union_rates.py::select_rate_row -> engine/cost_localizer.py labour line -> the landed cost total"
    - "data/union_rates/fringe_schedules.yaml -> a separate fringe Figure sibling of the wage Figure (never a multiplier baked into the wage)"
    - "sources/unions/*.pdf + sources/MANIFEST.yaml sha256 -> the only thing that entitles a rate row to `basis: sourced`"
  prohibitions:
    - "No AWS AI endpoint is called or imported — Textract especially; union rate cards are PDFs and Textract is the specific trap on this project."
    - "No new runtime dependency is added to pyproject.toml or uv.lock (D-57)."
    - "No runtime network call is made by any code path this phase adds (D-57) — rate-card acquisition is a one-time committed snapshot."
    - "No `.quantize()` call site outside engine/rounding.py::quantize_money."
    - "No YAML is loaded with anything other than `yaml.safe_load`."
    - "No fringe percentage sourced only from a payroll-vendor blog is tagged `basis: sourced` (PITFALLS E1, research row A1/A2)."
    - "No fringe amount is folded into the wage line (D-62)."
    - "No overtime, turnaround penalty, meal penalty or kit fee is modelled — they stay in PERMANENT_EXCLUSIONS (D-60)."
    - "No unquoted numeric value in any new YAML data file (RD-01)."
    - "No jurisdiction id string appears in engine/cost_localizer.py or engine/union_rates.py (JUR-05/D-53)."
---

<objective>
Widen the tracer's single crew line into real, per-union, per-department labour pricing with fringe and payroll burden as its own visible deduction line, and add Los Angeles as the second committed cost profile — a city with a full cost profile and no jurisdiction rule file, which is D-53's whole point.

Purpose: COST-02 and COST-03 are the largest cost lines in the model and the ones a producer will check first; a blended invented day rate would fail on sight.
Output: committed dated union rate snapshots with archived primary documents, a rate-row selector with a dated-range contract, and a labour plus fringe pair of Figures per department.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-CONTEXT.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-RESEARCH.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-PATTERNS.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-01-SUMMARY.md
@.planning/research/PITFALLS.md
@jurisdictions/SCOPE-FREEZE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire and commit dated union rate-card and fringe snapshots</name>
  <files>data/union_rates/iatse.yaml, data/union_rates/sag-aftra.yaml, data/union_rates/dga.yaml, data/union_rates/wga.yaml, data/union_rates/fringe_schedules.yaml, data/union_rates/us-ny-crew.yaml, sources/MANIFEST.yaml</files>
  <read_first>
    - data/crew_tiers.yaml (the header convention every new data file copies: a leading comment stating what the file is and that it must never declare a `confidence` or `status` key, a top-level `basis`, a `provenance_note`, quoted-string values)
    - data/union_rates/us-ny-crew.yaml (the tracer's single row, and whatever basis 04-01 assigned it)
    - sources/MANIFEST.yaml (the `path` / `url` / `retrieved_at` / `sha256` / `document_title` / `cited_for` record shape and the D-10 invariant its header states)
    - jurisdictions/us-ny.yaml (RD-01 quoted-string convention in a committed rule file)
    - .planning/phases/04-cost-localization-landed-cost-outputs/04-RESEARCH.md § Common Pitfalls Pitfall 1 and § Assumptions Log rows A1 and A2
    - .planning/research/PITFALLS.md Part E (E1 fringe and burden must be disclosed, E5 the general confidence discipline)
  </read_first>
  <action>
Fetch, archive and transcribe the union rate data. This is a one-time acquisition, not a runtime dependency (D-57).

Target sources, in order of preference — the union's own published document always wins:
- IATSE camera scale via the International Cinematographers Guild (Local 600) rate card at `icg600.com`; New York studio-mechanics scale (Local 52) and Los Angeles equivalents where published.
- SAG-AFTRA theatrical and low-budget schedules at `sagaftra.org`, plus its published Pension and Health contribution percentage.
- DGA published Pension and Health contribution percentage from the DGA's own agreement material at `dga.org`.
- WGA published minimums at `wga.org`.

For each document you actually obtain: save the bytes under `sources/unions/<YYYY-MM-DD>-<slug>.<ext>`, compute its sha256, and add a `sources/MANIFEST.yaml` entry with `url`, `retrieved_at`, `sha256`, `document_title` and a `cited_for` list naming the exact rows transcribed from it. PDF and document text extraction goes through a plain fetch and read — never AWS Textract or any other AWS AI endpoint; that is a Stage One disqualification on this project.

Write one YAML file per union under `data/union_rates/`. Each file: the `data/crew_tiers.yaml` header shape, a top-level `provenance_note`, and a `rows:` list where each row carries `row_id`, `union`, `local` (nullable), `region` (a plain region label such as `us-ny`-shaped city or region string used only as a data key, never branched on in engine code), `craft`, `rate` (quoted string), `rate_unit` (`day` or `hour` or `week`), `effective_from` and `effective_to` (quoted ISO dates; `effective_to` may be null for the current open row), `basis`, `source_url`, `date_checked`, and `method_note` (required whenever `basis` is not `sourced`).

`data/union_rates/fringe_schedules.yaml` holds the COST-03 half separately from wage scale: one entry per union carrying `pension_health_pct`, `payroll_tax_pct`, `other_burden_pct`, each a quoted string, each with its own `basis`, `source_url`, `date_checked` and `method_note`. Tag a percentage `sourced` ONLY when it comes from that union's own document archived in this task. A percentage you can only find on a payroll-vendor page is `estimated`, with `method_note` naming that page and stating that it is industry commentary rather than a union document — this is research rows A1 and A2, and mislabelling one of them is the single most likely honesty failure in this plan.

Where the tracer's `data/union_rates/us-ny-crew.yaml` row can be upgraded from `estimated` to `sourced` because you obtained the primary document, upgrade it and record the upgrade. Where it cannot, leave it `estimated` and add an entry to `.planning/WINDOWS.md` naming the row, the union, what was searched, and why no primary document was obtainable. Do not silently leave an unexplained `estimated` row.

Every dated-range convention in these files is closed-closed, matching WR-03 as recorded in `jurisdictions/SCOPE-FREEZE.md`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_source_truth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Each of `data/union_rates/iatse.yaml`, `sag-aftra.yaml`, `dga.yaml`, `wga.yaml`, `fringe_schedules.yaml` exists and every numeric value in it is a quoted string
    - `grep -v '^#' data/union_rates/*.yaml | grep -c 'status:'` returns 0
    - Every row whose `basis` is `sourced` has a non-null `source_url` and a `sources/MANIFEST.yaml` entry whose `cited_for` names it
    - Every row whose `basis` is not `sourced` has a non-empty `method_note`
    - Re-hashing every file listed in `sources/MANIFEST.yaml` reproduces its recorded `sha256`
    - `grep -rniE 'textract|bedrock|comprehend|rekognition|sagemaker' . --include='*.py' --include='*.yaml' --include='*.toml'` returns no hits
    - Any row left at `basis: estimated` because no primary document was obtainable has a matching entry in `.planning/WINDOWS.md`
  </acceptance_criteria>
  <precondition>Network access is available for a one-time document fetch; if a union site blocks the fetch, record the block in `.planning/WINDOWS.md` and tag the affected rows `estimated` rather than halting the plan.</precondition>
  <done>Union wage scale and fringe schedules exist as committed, dated, quoted-string snapshots whose `sourced` rows each trace to an archived primary document with a verified hash.</done>
</task>

<task type="auto">
  <name>Task 2: Labour and fringe as two separate Figures, and the Los Angeles cost profile</name>
  <files>engine/union_rates.py, engine/cost_profile.py, engine/cost_localizer.py, data/cost_profiles/us-ny-new-york.yaml, data/cost_profiles/us-ca-los-angeles.yaml</files>
  <read_first>
    - engine/cost_localizer.py (as created by 04-01 — the `localize` signature, `LocalizedBudget`, `_find_line_by_label`, the spend_class-derived SpendBreakdown)
    - engine/cost_profile.py (as created by 04-01 — `CostLine`, `CityCostProfile`, the sourced-requires-source_url validator)
    - engine/budget.py (the per-department quantity lines and their account tags)
    - engine/rounding.py (`quantize_money`, the single pinned call site)
    - engine/credit.py lines 38-92 (`lookup_flat_rate_by_band`, `blend_two_rates_by_ceiling` — the half-open band-lookup shape a rate selector must not accidentally contradict)
    - engine/credit.py lines 111-145 (`_loanout_schedule_bands_overlap`, `_check_loanout_schedule_for_overlaps` — the existing dated-band overlap guard to mirror)
    - engine/net_cash.py lines 100-124 (`_deduct_audit_fee` — the established pattern for showing a deduction as its own visible step rather than a haircut baked into a headline)
    - data/cost_profiles/us-ny-new-york.yaml (as created by 04-01)
    - data/union_rates/*.yaml (as created in Task 1)
  </read_first>
  <action>
**`engine/union_rates.py` — the rate-row selector.** A local `StrictModel`, a module-anchored `UNION_RATES_DIR = Path(__file__).resolve().parents[1] / "data" / "union_rates"`, Pydantic models for a rate row and a fringe schedule entry, and `load_union_rates()` / `load_fringe_schedules()` using `yaml.safe_load`. `select_rate_row(rows, *, region, craft, on_date) -> RateRow` selects by plain string equality on `region` and `craft` with no normalization, then by the closed-closed dated range: a row matches when `effective_from <= on_date` and (`effective_to` is null or `on_date <= effective_to`). No match raises `ValueError` naming region, craft and date — never falls back to the nearest row and never picks the newest. Mirror `_check_loanout_schedule_for_overlaps`: raise at load time if two rows for the same region and craft have overlapping dated ranges, including the open-ended-versus-dated case.

Derive the shoot's `on_date` from `ProductionSpec.start_quarter` plus `start_year` as the first day of the quarter's first month, and state that derivation in the labour Figure's derivation lines — the reader must never have to infer which date selected the rate.

**`engine/cost_localizer.py` — the labour path.** For each department line in the `CanonicalBudget`, look up the profile's declared craft-to-union-and-craft mapping, select the rate row, and emit TWO sibling Figures per department:
1. a wage Figure: `quantize_money(department_days * Decimal(row.rate))`, derivation naming headcount, days, rate, rate unit, the selected row's `row_id`, its `effective_from`, and the union document URL; `basis` copied from the row; `confidence="researched"`;
2. a fringe Figure, labelled distinctly (for example "Fringe and payroll burden — <department>"), computed as the wage value times the summed fringe percentages for that union from `fringe_schedules.yaml`, quantized once, carrying the wage Figure as its single `inputs` entry, its own `basis` from the fringe entry, and a derivation line disclosing every percentage component by name and value.

The fringe Figure is never merged into the wage Figure and never applied as a silent multiplier — this is D-62 and it is the identical treatment `_deduct_audit_fee` already gives the incentive side. Both Figures are independent entries in `LocalizedBudget.lines`, so `combined_basis` sees them both.

**Profile schema extension (`engine/cost_profile.py`).** Add to `CityCostProfile` a `labour:` block declaring `region` (the data key matched against union rate rows), and a `crafts:` mapping from department name to `{union, craft}`. Add a validator asserting every department named in `data/crew_tiers.yaml`'s `departments:` block has a craft mapping, so a new department cannot silently price at zero.

**`data/cost_profiles/us-ca-los-angeles.yaml`.** A full Los Angeles profile: `city_id: us-ca-los-angeles`, `jurisdiction_id: null` — Los Angeles has no rule file in this phase and D-53 says a cost profile does not need one — `currency: "USD"`, a `provenance_note` stating why `jurisdiction_id` is null and that California's rule file is Phase 5's work, and the same `labour` block pointing at LA-region rate rows. Extend `engine/city_profile_lookup.py`'s allow-list with Los Angeles aliases (`los angeles`, `la`, `hollywood`, `burbank`, `culver city`, plus the `, ca` / `, california` suffix handling matching the existing New York suffix pattern).

Remove `labour` and `fringe` from the aggregator's `not_priced` output for both cities by virtue of the lines now existing — do not special-case the list.

Nothing in `engine/union_rates.py` or `engine/cost_localizer.py` may contain a jurisdiction id string literal; `region` is a profile-declared data key, and the engine compares it, never branches on its value.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine_union_rates.py tests/test_engine_cost_localizer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `engine/union_rates.py` contains `def select_rate_row(` and raises `ValueError` for a date covered by no row
    - Overlapping dated ranges for the same region and craft raise at load time
    - Every localized labour department produces exactly two Figures whose labels differ, and the fringe Figure carries the wage Figure in its `inputs`
    - The wage Figure's `value` is unchanged when fringe is removed — proving fringe is additive, not baked in
    - `data/cost_profiles/us-ca-los-angeles.yaml` loads through `load_cost_profile` with `jurisdiction_id` equal to `None`
    - `grep -nE '"us-ny"|"us-ca"|us-ny|us-ca' engine/cost_localizer.py engine/union_rates.py` returns no hits
    - `localize` output for New York and for Los Angeles is produced from the same `CanonicalBudget` object (assert object identity in the test) — COST-01 stays true
    - `city_costs` for a submission naming both cities returns two entries, and Los Angeles carries `incentive_state` of `not_modelled` with a plain-words reason, never a zero incentive figure
  </acceptance_criteria>
  <reversibility rating="reversible">The labour and fringe Figure pair is additive to the existing line list; collapsing them later is a local change in one module.</reversibility>
  <done>Both cities price labour from dated union rate rows with fringe as its own visible line, and Los Angeles proves a cost profile needs no rule file.</done>
</task>

<task type="auto">
  <name>Task 3: Boundary and precision coverage for labour pricing</name>
  <files>tests/test_engine_union_rates.py, tests/test_engine_cost_localizer.py, .planning/WINDOWS.md</files>
  <read_first>
    - engine/union_rates.py and engine/cost_localizer.py (as written in Task 2)
    - engine/rounding.py (the ROUND_HALF_UP contract and the Christmas Always half-dollar note in its docstring)
    - tests/test_engine_rounding.py (the existing property-test shape for the pinned rounding call site)
    - tests/test_engine_net_cash.py lines 1-30 (the fixture-driven, sorted-glob, fail-loud test discipline)
    - .planning/WINDOWS.md (the open-entry format)
  </read_first>
  <action>
Write the boundary and precision tests the edge probe named for COST-02, and record any unresolved sourcing gap.

**Boundary (dated rate rows).** For a rate row with `effective_from` of a fixed date D and `effective_to` of a later date E, assert: a shoot date exactly equal to D selects that row; exactly equal to E selects that row (closed-closed); one day before D selects the prior row when one exists and raises when none does; one day after E raises when no successor exists and selects the successor when one does. Assert the raise message names region, craft and the date — a silent nearest-row fallback is the failure this test exists to prevent.

**Precision.** Assert labour cost arithmetic stays in `Decimal` end to end: construct a rate and a day count whose product lands exactly on a half-dollar boundary and assert the result rounds up, matching `quantize_money`'s pinned ROUND_HALF_UP rather than Python's ambient ROUND_HALF_EVEN. Assert the wage Figure's value equals `quantize_money(days * Decimal(rate))` computed once — not a quantize of already-quantized parts — by building a case where quantizing twice would differ by one dollar. Assert `grep`-free structurally: that no float ever enters the path, by asserting `isinstance(figure.value, Decimal)` and that constructing a profile whose `rate` is an unquoted YAML number is rejected by the schema.

**Fringe precision.** Assert the fringe Figure equals `quantize_money(wage_value * summed_pct)` with the sum of percentages taken in `Decimal` before multiplication, not a sequence of successive multiplications — build a case where the two orders differ and assert the declared one.

**Sourcing record.** For every fringe percentage or rate row still tagged `estimated`, confirm `.planning/WINDOWS.md` carries an entry naming it. Add a test asserting that every row in `data/union_rates/*.yaml` tagged `basis: sourced` has a `sources/MANIFEST.yaml` entry naming it in `cited_for` — so a future rate edit cannot promote a row to `sourced` without archiving its document.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_engine_union_rates.py -q` exits 0 and contains a test whose name contains `boundary` covering exactly-on, one-before and one-after
    - A test asserts a half-dollar labour product rounds up, and fails if `quantize_money` is replaced with a bare `.quantize()` using the default context
    - A test asserts every `basis: sourced` union row is named in a `sources/MANIFEST.yaml` `cited_for` list
    - `uv run --frozen pytest tests/ -q` exits 0
    - `.planning/WINDOWS.md` has an entry for every rate or fringe value left at `basis: estimated`
  </acceptance_criteria>
  <done>The dated-range and rounding contracts for labour are asserted, not assumed, and every un-sourced figure is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| union website → archived document → `data/union_rates/*.yaml` | An external document crosses into a committed figure; transcription is where a wrong number enters |
| committed YAML → `engine/union_rates.py` loaders | Repo-controlled data crosses into typed models |
| labour Figure tree → JSON response | A `sourced` claim on a rate row is a citation the public repo asserts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Spoofing | rate-card provenance | high | mitigate | `basis: sourced` requires an archived document under `sources/` with a `sources/MANIFEST.yaml` sha256 and a `cited_for` naming the row; Task 3 asserts this in CI so a row cannot be promoted without its document. |
| T-04-07 | Tampering | `data/union_rates/*.yaml` hand-edit | medium | mitigate | Pydantic `extra="forbid"` plus load-time overlap detection on dated ranges; a malformed or overlapping table fails the suite rather than silently selecting a row. |
| T-04-08 | Repudiation | fringe percentage mislabelled `sourced` | high | mitigate | `method_note` is required whenever `basis` is not `sourced`; research rows A1/A2 name the specific payroll-vendor pages that must not be promoted; unresolved cases go to `.planning/WINDOWS.md`. |
| T-04-09 | Tampering | PDF extraction path | high | mitigate | Document text is read by plain fetch only. AWS Textract and every other AWS AI endpoint is forbidden by name; the existing `vendor-scan` CI job greps the source tree and Task 1 adds an explicit grep acceptance criterion. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed (D-57); `pyproject.toml` and `uv.lock` are not in `files_modified`; the existing `lockfile-scan` CI job blocks any addition. |
</threat_model>

## Artifacts this phase produces

- `engine/union_rates.py`: `UNION_RATES_DIR`, `RateRow`, `FringeSchedule`, `load_union_rates`, `load_fringe_schedules`, `select_rate_row`
- `engine/cost_profile.py`: `CityCostProfile.labour`, `LabourBlock`, `CraftMapping`
- `data/union_rates/iatse.yaml`, `sag-aftra.yaml`, `dga.yaml`, `wga.yaml`, `fringe_schedules.yaml`
- `data/cost_profiles/us-ca-los-angeles.yaml`
- `sources/unions/` archived documents and their `sources/MANIFEST.yaml` entries
- `tests/test_engine_union_rates.py`
- `engine/city_profile_lookup.py`: Los Angeles alias entries

## Flagged Assumptions — unresolved edge probes

| Requirement | Probe | Status |
|---|---|---|
| COST-03 | unclassified — review manually | unresolved — carried into `/gsd-verify-work` as an explicit assumption |

Research assumptions carried into execution and NOT auto-dismissed: **A1** (the 35–45% blanket IATSE fringe figure is payroll-vendor commentary, not a union document) and **A2** (the DGA 22.25% Pension and Health rate needs primary-document confirmation). Both must resolve to a `basis` decision recorded in the SUMMARY, never to a silent `sourced` tag.

<verification>
- `uv run --frozen pytest tests/ -q` exits 0
- Every `basis: sourced` row in `data/union_rates/` traces to an archived document with a verified sha256
- `grep -rn "\.quantize(" engine/ | grep -v "engine/rounding.py"` returns no hits
- `grep -rn "yaml.load\|yaml.unsafe_load" engine/` returns no hits
- `git diff --stat pyproject.toml uv.lock` is empty
- A New York plus Los Angeles submission returns two `city_costs` entries whose labour and fringe lines are separate Figures
</verification>

<success_criteria>
- Labour prices from dated union rate rows, never a blended invented rate (COST-02)
- Fringe and payroll burden is a separate, sourced-where-published, method-disclosed line (COST-03)
- Los Angeles has a full cost profile and no rule file, proving D-53
- Every un-sourced rate or percentage is recorded in `.planning/WINDOWS.md`
</success_criteria>

<output>
Create `.planning/phases/04-cost-localization-landed-cost-outputs/04-02-SUMMARY.md` when done. Record: which union documents were actually obtained and archived; the final `basis` assigned to every fringe percentage and why; the resolution of research assumptions A1 and A2; and the new `total_landed_cost` Decimal for the fixed test spec against 04-01's recorded value, with the delta explained.
</output>
</content>
