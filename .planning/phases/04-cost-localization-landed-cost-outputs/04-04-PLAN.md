---
phase: 04-cost-localization-landed-cost-outputs
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - data/facilities/us-ny-new-york.yaml
  - data/facilities/us-ca-los-angeles.yaml
  - data/tax_exemptions/us-ny-new-york.yaml
  - data/tax_exemptions/us-ca-los-angeles.yaml
  - engine/facilities.py
  - engine/exemptions.py
  - engine/cost_profile.py
  - engine/cost_localizer.py
  - engine/landed_cost.py
  - data/cost_profiles/us-ny-new-york.yaml
  - data/cost_profiles/us-ca-los-angeles.yaml
  - tests/test_engine_facilities.py
  - tests/test_engine_exemptions.py
  - tests/test_engine_cost_localizer.py
  - tests/test_engine_landed_cost.py
autonomous: true
requirements: [COST-06, INC-10]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Stage, equipment, permit, location and trucking costs are all priced, and every one of them is labelled `estimated` or `modelling_assumption` — never `sourced` (COST-06)."
    - "A facilities figure derived from a named public anchor states the anchor and discloses a range rather than presenting a single marketing listing as typical (research row A5)."
    - "The schema rejects `basis: sourced` on any facilities line at load time, so a future edit cannot promote one silently."
    - "Sales-tax and hotel-occupancy exemptions appear as their own named, stackable, sourced cost-reduction Figures attached to the cost line they reduce (INC-10, D-76)."
    - "An exemption Figure is never reachable from the incentive Figure DAG returned by `price_jurisdiction`, and adding an exemption never increases the reported gross credit (D-76)."
    - "After this plan, `not_priced` is empty for New York and Los Angeles, and `permanent_exclusions` still names overtime, turnaround penalties, meal penalties, kit fees, non-union local differentials and negotiated hotel rates (D-60)."
  artifacts:
    - engine/facilities.py
    - engine/exemptions.py
    - data/facilities/us-ny-new-york.yaml
    - data/facilities/us-ca-los-angeles.yaml
    - data/tax_exemptions/us-ny-new-york.yaml
    - data/tax_exemptions/us-ca-los-angeles.yaml
    - tests/test_engine_facilities.py
    - tests/test_engine_exemptions.py
  key_links:
    - "data/facilities/*.yaml -> engine/facilities.py -> five cost lines in the localized budget -> the `not_priced` list emptying"
    - "data/tax_exemptions/*.yaml -> engine/exemptions.py -> a reduction Figure attached to a specific cost line, and provably absent from the incentive tree"
    - "engine/cost_profile.py validator rejecting `basis: sourced` on facilities -> the structural guarantee behind COST-06"
  prohibitions:
    - "No AWS AI endpoint is called or imported."
    - "No new runtime dependency is added to pyproject.toml or uv.lock (D-57)."
    - "No runtime network call is made by any code path this phase adds (D-57)."
    - "No facilities line carries `basis: sourced` — no standardized public rate card exists for stages, equipment, permits, locations or trucking (COST-06, research Pitfall 4)."
    - "No exemption value is added to, netted into, or otherwise folded into the incentive figure (D-76)."
    - "No unpriced category is rendered as a zero cost line (D-60)."
    - "No `.quantize()` call site outside engine/rounding.py::quantize_money."
    - "No YAML is loaded with anything other than `yaml.safe_load`."
    - "No unquoted numeric value in any new YAML data file (RD-01)."
    - "No jurisdiction id string appears in engine/facilities.py, engine/exemptions.py or engine/cost_localizer.py (JUR-05/D-53)."
---

<objective>
Close the last five cost categories and land INC-10's stackable exemptions on the cost side of the line, where the requirement's own wording puts them.

Purpose: this is the phase's most honestly under-sourced category (04-RESEARCH.md says so explicitly), so the value here is not the numbers — it is that every number says exactly what it is. And INC-10 is the one requirement whose obvious wrong implementation would corrupt the reproducible credit figure.
Output: five facilities cost lines per city labelled non-sourced by schema, exemptions as their own reduction Figures, and an empty `not_priced` list for both US floor cities.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-CONTEXT.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-RESEARCH.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-PATTERNS.md
@.planning/phases/04-cost-localization-landed-cost-outputs/04-03-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Facilities reference tables, non-sourced by schema</name>
  <files>data/facilities/us-ny-new-york.yaml, data/facilities/us-ca-los-angeles.yaml, engine/facilities.py, engine/cost_profile.py, tests/test_engine_facilities.py</files>
  <read_first>
    - .planning/phases/04-cost-localization-landed-cost-outputs/04-RESEARCH.md § Common Pitfalls Pitfall 4 (no standardized public rate card exists; a studio marketing page is not a citation) and § Assumptions Log row A5 (a disclosed range is more honest than a single anchor)
    - .planning/research/PITFALLS.md Part E items E3 and E5
    - data/crew_tiers.yaml (the header convention, `basis: modelling_assumption`, `provenance_note`)
    - engine/cost_profile.py (as modified by 04-03 — `CostLine`, `CityCostProfile`, the labour and travel blocks, the existing sourced-requires-source_url validator)
    - engine/union_rates.py (the loader shape and the module-anchored directory constant to mirror)
    - engine/rounding.py
  </read_first>
  <action>
Write `data/facilities/us-ny-new-york.yaml` and `data/facilities/us-ca-los-angeles.yaml`, one entry per category: `stages`, `equipment`, `permits`, `locations`, `trucking`. Each entry carries `rate_low` and `rate_high` as quoted strings, `rate_unit` (`shoot_day`, `week` or `flat`), `basis` (`estimated` or `modelling_assumption` only), `source_url` and `date_checked` where an anchor exists, `anchor_note` naming the specific public listing or published rate the range was drawn from, and `method_note` disclosing how the range was arrived at.

Where a defensible public anchor exists (a named studio's published day rate, a city film office's published permit fee schedule — permit fees are the one category with a genuine chance of a government-published number), use `basis: estimated` and name the anchor. Where none exists, use `basis: modelling_assumption` and say so in `method_note`. Never `sourced`: no standardized public rate card exists for any of these, and presenting a marketing listing as a citation is the failure research Pitfall 4 names.

If a city film office publishes a permit fee schedule that genuinely is a primary government document, you may archive it under `sources/` with a MANIFEST entry and cite it — but the derived cost line still uses a range across production types and stays `estimated`, because the fee schedule prices a permit, not this production's permit count. State that reasoning in `method_note`.

**Pricing rule.** A facilities line's reported value is the midpoint of `rate_low` and `rate_high` times the driving quantity, and the Figure's derivation states the low bound, the high bound, that the midpoint was taken, and the anchor — a bare midpoint with the range hidden would be exactly the point estimate research row A5 warns against. Where the range is wide enough that a midpoint misleads, prefer a wider structural answer: emit the line at the low bound and record the high bound in the derivation as the stated upper bound. Pick one treatment, apply it uniformly across all five categories in both cities, and name the choice in the SUMMARY.

**`engine/facilities.py`.** A local `StrictModel`, module-anchored `FACILITIES_DIR`, `load_facilities(facilities_id)` resolving through a committed id-to-path dict with plain string equality, and `facilities_lines(table, budget, currency) -> tuple[Figure, ...]` returning one Figure per category. Add a Pydantic validator on the facilities entry model that rejects `basis: "sourced"` outright with a message naming COST-06 — the structural guarantee, not a convention.

Add a `facilities_id` field to `CityCostProfile` and wire the id for both cities.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine_facilities.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both facilities YAML files exist with all five categories and every numeric value quoted
    - Loading a facilities entry with `basis: "sourced"` raises a validation error naming COST-06
    - Every facilities entry has a non-empty `method_note`, and every entry with a `source_url` also has an `anchor_note`
    - Every facilities Figure's derivation names both the low bound and the high bound, whichever pricing treatment was chosen
    - `grep -nE '"us-ny"|"us-ca"' engine/facilities.py` returns no hits
    - `uv run pytest tests/test_engine_facilities.py -q` exits 0
  </acceptance_criteria>
  <done>All five facilities categories price for both US cities, with the never-sourced rule enforced by the schema rather than by discipline.</done>
</task>

<task type="auto">
  <name>Task 2: Wire facilities into the localized budget and empty the not-priced list</name>
  <files>engine/cost_localizer.py, engine/landed_cost.py, data/cost_profiles/us-ny-new-york.yaml, data/cost_profiles/us-ca-los-angeles.yaml, tests/test_engine_cost_localizer.py, tests/test_engine_landed_cost.py</files>
  <read_first>
    - engine/cost_localizer.py (as modified by 04-03 — the labour, fringe, housing, per-diem and flight paths)
    - engine/landed_cost.py (as modified by 04-03 — `COST_CATEGORIES`, `PERMANENT_EXCLUSIONS`, `not_priced`, `quarter_variant_lines`, `seasonality_state`)
    - engine/facilities.py (as created in Task 1)
    - engine/budget.py (the driving quantities: shoot days, department headcounts)
    - data/cost_profiles/us-ny-new-york.yaml and us-ca-los-angeles.yaml as they stand after 04-03
  </read_first>
  <action>
Call `facilities_lines` from `localize` and append its five Figures to `LocalizedBudget.lines`, each declaring its `spend_class` and `account` from the profile so the SpendBreakdown and the chart-of-accounts tagging stay data-driven.

Driving quantities: stages priced per stage shoot day (`spec.shoot_days_stage`), locations and permits per location shoot day (`spec.shoot_days_location`), equipment and trucking per total shoot day. State the driver in each Figure's derivation — a reader must be able to see which shoot-day count moved which line, because that is exactly what the sensitivity output in plan 04-07 will perturb.

With all ten categories now present, `not_priced` must compute to empty for both cities. Do not hardcode an empty tuple — it must fall out of `COST_CATEGORIES` minus the categories actually present, so a future profile missing a category is caught. Add a test asserting that deleting a category from a test profile makes it reappear in `not_priced`, proving the list is a measurement rather than a claim.

`permanent_exclusions` is unchanged and must still name overtime, turnaround penalties, meal penalties, kit fees, non-union local differentials and negotiated hotel rates — these are never modelled in Accounts (D-60) and the total must keep saying so.

Confirm `combined_basis` over the full ten-line set now returns the weakest tier present. With facilities lines at `modelling_assumption`, the New York and Los Angeles totals must both report `modelling_assumption` — assert it. A total reporting anything stronger while containing an assumption is the D-59 failure this phase exists to prevent.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine_cost_localizer.py tests/test_engine_landed_cost.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `not_priced` is empty for both New York and Los Angeles
    - A test that removes one category from a synthetic profile sees that category reappear in `not_priced`
    - `permanent_exclusions` names all six D-60 items
    - Both cities' `total_landed_cost.basis` equals `"modelling_assumption"`
    - Each facilities Figure's derivation names which shoot-day count drove it
    - `uv run --frozen pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>Every cost category in the canonical vocabulary is priced for both US floor cities, and the total honestly reports the weakest basis it contains.</done>
</task>

<task type="auto">
  <name>Task 3: INC-10 — tax exemptions as stackable cost reductions that never touch the credit</name>
  <files>data/tax_exemptions/us-ny-new-york.yaml, data/tax_exemptions/us-ca-los-angeles.yaml, engine/exemptions.py, engine/cost_profile.py, engine/cost_localizer.py, tests/test_engine_exemptions.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 55 (INC-10 verbatim — "separate stackable cost reductions where they exist")
    - .planning/phases/04-cost-localization-landed-cost-outputs/04-CONTEXT.md § D-76
    - engine/net_cash.py lines 100-124 (`_deduct_audit_fee` — a deduction shown as its own visible step with its own derivation line, never a haircut baked into a headline)
    - engine/pipeline.py lines 209-277 (`price_jurisdiction`'s returned Figure DAG — the tree the exemption must provably never enter)
    - engine/cost_localizer.py (as modified in Task 2)
    - jurisdictions/us-ny.yaml (New York's programme terms, for confirming the credit figure is unaffected)
  </read_first>
  <action>
Write `data/tax_exemptions/<city>.yaml` per city, each entry carrying `exemption_id`, `label`, `applies_to_category` (one of the canonical `COST_CATEGORIES`), `kind` (`sales_tax` or `hotel_occupancy` or another declared kind), `rate` (quoted string), `basis`, `source_url`, `date_checked`, `method_note`, and `eligibility_note` in plain words. An exemption a city does not offer is simply absent from its file — never an entry with a zero rate, which would assert the exemption exists and is worth nothing.

New York's sales-tax exemption for production equipment and its hotel-occupancy treatment are genuinely published by the state, so these can be `basis: sourced` if you archive the published guidance under `sources/` with a `sources/MANIFEST.yaml` entry and cite the exact provision. If you cannot obtain the primary document, tag `estimated` and record the gap in `.planning/WINDOWS.md`.

**`engine/exemptions.py`.** `load_exemptions(exemptions_id)` through a committed id-to-path dict, and `exemption_reductions(table, cost_lines, currency) -> tuple[Figure, ...]` returning one reduction Figure per applicable exemption. Each reduction Figure: a negative `value` computed as `quantize_money(-(matched_line.value * Decimal(entry.rate)))`, a label naming the exemption and the line it reduces, the matched cost Figure as its single `inputs` entry, `basis` and `caveat` from the entry, and a derivation stating the rate, the line it applied to, and the eligibility note. Match the cost line by `applies_to_category` using plain string equality against the canonical category vocabulary — never by positional index and never by substring.

Reductions are appended to `LocalizedBudget.lines` as first-class lines, so they enter the cost total through the same summation every other line uses and appear individually in the `inputs` tree. Multiple exemptions applying to the same category stack — each is its own Figure, applied to the pre-reduction line value, and the derivation of each says so, so the stacking order is visible rather than implied.

**The D-76 guarantee, proven.** Add `tests/test_engine_exemptions.py` asserting:
1. Every exemption reduction Figure's `figure_id` is absent from the recursive `inputs` DAG of `price_jurisdiction`'s returned `total_net_cash` Figure — collect the incentive DAG's ids into a set and assert disjointness.
2. Pricing the same spec with the exemptions table present and with it absent produces an identical gross-credit `Decimal` — or, where the exemption's declared `spend_class` legitimately reduces qualified spend, a strictly lower one, never a higher one; assert the direction and assert the derivation names the reason.
3. The cost total differs between the two runs by exactly the summed reduction amount.
4. An exemption whose `applies_to_category` matches no present cost line raises naming the exemption id, rather than being silently dropped.

Record in the SUMMARY which of the two treatments in point 2 each committed exemption actually takes, and why — that is a modelling decision a reader is entitled to see stated, not inferred.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_engine_exemptions.py -q` exits 0
    - A test asserts the exemption Figure ids and the incentive DAG's Figure ids are disjoint sets
    - A test asserts the gross credit is never greater with exemptions than without
    - A test asserts the cost total delta equals the summed reduction amount exactly
    - An exemption naming an absent category raises rather than being dropped
    - Two exemptions on the same category both appear as separate Figures in the tree
    - Every exemption tagged `basis: sourced` has an archived document with a `sources/MANIFEST.yaml` entry; every other is recorded in `.planning/WINDOWS.md`
    - `uv run --frozen pytest tests/ -q` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">Exemption reductions are additional entries in an existing line list; removing them is a local change in one module.</reversibility>
  <done>Exemptions reduce the cost lines they apply to as their own named stackable figures, and a test proves they never enter the incentive figure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public marketing listing → `data/facilities/**` | An unstandardized commercial figure crosses into a cost line; the tamper here is provenance inflation |
| `data/tax_exemptions/**` → the cost total and, separately, the incentive total | The one place a cost-side number could corrupt the reproducible credit |
| facilities and exemption Figures → JSON response | A `sourced` claim the repo cannot back is a public credibility failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Spoofing | facilities provenance inflation | high | mitigate | A Pydantic validator rejects `basis: "sourced"` on any facilities entry at load time with a message naming COST-06 — a schema guarantee, not a review convention. |
| T-04-16 | Tampering | exemption leaking into the incentive figure | high | mitigate | `tests/test_engine_exemptions.py` asserts the exemption Figure ids are disjoint from `price_jurisdiction`'s returned DAG, and that gross credit is never increased. |
| T-04-17 | Tampering | exemption matched to the wrong cost line | medium | mitigate | Matching is plain string equality against the closed `COST_CATEGORIES` vocabulary; a non-matching exemption raises naming its id rather than being silently dropped. |
| T-04-18 | Repudiation | a wide facilities range presented as a point estimate | medium | mitigate | Every facilities Figure's derivation states both bounds and the treatment applied; the treatment is uniform across all five categories and recorded in the SUMMARY. |
| T-04-19 | Denial of Service | malformed committed table | medium | mitigate | Pydantic `extra="forbid"` and explicit raises at load time; failures surface in CI, not mid-request. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed (D-57); `pyproject.toml` and `uv.lock` untouched; `lockfile-scan` CI job blocks any addition. |
</threat_model>

## Artifacts this phase produces

- `engine/facilities.py`: `FACILITIES_DIR`, `FACILITIES_PATH_BY_ID`, `FacilitiesEntry`, `FacilitiesTable`, `load_facilities`, `facilities_lines`
- `engine/exemptions.py`: `EXEMPTIONS_DIR`, `EXEMPTIONS_PATH_BY_ID`, `ExemptionEntry`, `load_exemptions`, `exemption_reductions`
- `engine/cost_profile.py`: `CityCostProfile.facilities_id`, `CityCostProfile.exemptions_id`
- `data/facilities/us-ny-new-york.yaml`, `data/facilities/us-ca-los-angeles.yaml`
- `data/tax_exemptions/us-ny-new-york.yaml`, `data/tax_exemptions/us-ca-los-angeles.yaml`
- `tests/test_engine_facilities.py`, `tests/test_engine_exemptions.py`

## Flagged Assumptions — unresolved edge probes

| Requirement | Probe | Status |
|---|---|---|
| COST-06 | unclassified — review manually | unresolved — carried into `/gsd-verify-work` as an explicit assumption |
| INC-10 | unclassified — review manually | unresolved — carried into `/gsd-verify-work` as an explicit assumption |

Research assumption carried into execution: **A5** — stage, equipment, permit and trucking anchor figures found during research are individual marketing listings with no standardization. The plan's answer is a disclosed range plus a uniform, named treatment; the SUMMARY must state which treatment was chosen and why.

<verification>
- `uv run --frozen pytest tests/ -q` exits 0
- `not_priced` is empty for New York and Los Angeles; `permanent_exclusions` still names all six D-60 items
- Both cities' total landed cost reports `basis: modelling_assumption`
- No facilities entry loads with `basis: sourced`
- Exemption Figure ids are disjoint from the incentive Figure DAG
- `grep -rn "\.quantize(" engine/ | grep -v "engine/rounding.py"` returns no hits
- `git diff --stat pyproject.toml uv.lock` is empty
</verification>

<success_criteria>
- All five facilities categories price for both US cities, never claiming to be sourced (COST-06)
- Exemptions reduce cost lines as separate stackable figures and provably never touch the credit (INC-10)
- The `not_priced` list is empty because every category is genuinely priced, not because it was emptied
</success_criteria>

<output>
Create `.planning/phases/04-cost-localization-landed-cost-outputs/04-04-SUMMARY.md` when done. Record: the uniform facilities pricing treatment chosen (midpoint or low-bound) and why; which exemptions were sourced to a primary document and which were not; the declared qualified-spend treatment for each exemption; and the new `total_landed_cost` Decimal for both cities against 04-03's value, with the delta explained.
</output>
</content>
