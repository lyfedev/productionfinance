---
phase: 01-foundations-source-truth-deploy-path
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/hosting.env
  - deploy/README.md
autonomous: false
requirements: [SHP-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The subdomain label is confirmed by the developer before any DNS record is created — D-14 states explicitly that `prodfin.vockell.com` is an assumption, not a decision"
    - "`deploy/hosting.env` records the confirmed hostname, the static IP 35.165.60.123, the Lightsail instance name, the app port 8000 and the SSH key path, as shell-sourceable values every later deploy task reads"
    - "The DNS zone for vockell.com is documented as hosted at Register.com (Network Solutions), not Route 53 — so the record is created in the registrar's panel and no `aws route53` call is attempted (D-15, resolved by the research)"
    - "`dig +short A <subdomain>` returns exactly the one static IP 35.165.60.123; verification asserts the returned set contains that single address rather than depending on answer order"
    - "Before the record exists, `dig +short A <subdomain>` returns empty output, and the verification treats empty as not-yet-propagated rather than as success"
    - "The new subdomain A record is a sibling of the existing vockell.com apex A record — creating it leaves the apex record and its value unchanged, confirmed by re-querying the apex after the change"
    - "The DNS record is created before the instance resize, because propagation is the only clock in this phase outside our control (D-21)"
    - "`deploy/hosting.env` contains no secret — hostname, public IP, instance name, port and a key path only — and is deliberately tracked in the public repository"
  artifacts:
    - path: "deploy/hosting.env"
      provides: "Shell-sourceable host facts consumed by deploy/deploy.sh, the systemd unit, the Apache vhost and every SSH command in plans 01-07 through 01-09"
      contains: "PRODFIN_HOST"
    - path: "deploy/README.md"
      provides: "The deploy-path runbook: DNS zone host, the exact A-record specification, the D-21 ordering, and the rollback position at each step"
  key_links:
    - from: "deploy/hosting.env"
      to: "deploy/README.md"
      via: "the runbook explains each variable the env file records and where it came from"
      pattern: "PRODFIN_HOST"
    - from: "deploy/hosting.env"
      to: "the Register.com DNS zone"
      via: "PRODFIN_HOST names the record that must resolve to PRODFIN_STATIC_IP"
      pattern: "35.165.60.123"
  prohibitions:
    - requirement_id: "SHP-03"
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT modify, replace or delete any existing DNS record in the vockell.com zone while adding the project's subdomain — the apex record serves someone's live personal site, and the only permitted change is the addition of one new sibling record"
---

<objective>
Confirm the subdomain name, record the host facts every later deploy task reads, and get the DNS record created — first, because propagation is the one clock this phase does not control.

Purpose: D-21 fixes Track B's ordering as DNS record, then resize, then Python, then systemd, then Apache proxy and TLS, and puts DNS first for exactly this reason. A working application with no reachable URL fails the submission. D-14 is explicitly not delegated discretion: the subdomain label is an assumption awaiting one word of confirmation, and it gates everything downstream that names a hostname. D-15 asked where the DNS zone lives; the research answered it by live lookup — Register.com, not Route 53 — which makes this a credentialed manual step in a registrar panel rather than an AWS CLI call.

Output: A confirmed hostname, `deploy/hosting.env` carrying it alongside the static IP and instance facts, `deploy/README.md` as the deploy runbook, and an A record resolving to 35.165.60.123 with the vockell.com apex untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundations-source-truth-deploy-path/01-CONTEXT.md
@.planning/phases/01-foundations-source-truth-deploy-path/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Confirm the subdomain label for the hosted ProductionFinance URL</decision>
  <context>
    CONTEXT.md D-14 is one of exactly two items in this phase that Claude explicitly may not decide. It reads: `prodfin.vockell.com` is an assumption, not a confirmed decision — the subdomain was never named, and it must be confirmed before the DNS record is created. It is one word, and it is the only item on this phase's critical path carrying a propagation clock, so it is asked first rather than discovered later.

    What is already settled, from a live lookup in 01-RESEARCH.md § SHP-03: the zone for `vockell.com` is hosted at Register.com (nameservers dns105 and dns106.register.com, registrar Register.com - Network Solutions, LLC). It is not in Route 53 and is not managed through the AWS account at all. The apex `vockell.com` already resolves to `35.165.60.123`, which matches the Lightsail static IP recorded in PROJECT.md — so the box's identity is confirmed. Whatever label you choose becomes a new A record in that Register.com zone pointing at the same address.

    Where the name shows up once chosen: the Apache vhost `ServerName`, the Let's Encrypt certificate's subject, the hackathon submission URL a judge visits, and every verification command in plans 01-07 through 01-09. Changing it later means a new DNS record with a fresh propagation wait, a new certificate issuance against the live box, and a vhost edit — recoverable, but not free, and least free on day 15.

    Nothing else in this phase is blocked on this answer: plans 01-01 through 01-05 and the instance resize all proceed independently.
  </context>
  <options>
    <option id="prodfin">
      <name>prodfin.vockell.com (the CONTEXT.md placeholder)</name>
      <pros>Short, already written throughout the planning documents, matches the project's internal shorthand. No ambiguity about what it points at.</pros>
      <cons>"prodfin" is internal shorthand rather than a name a judge would recognise from the submission title.</cons>
    </option>
    <option id="productionfinance">
      <name>productionfinance.vockell.com</name>
      <pros>Matches the project name exactly, so the URL a judge visits reads as the thing they were told to look at. Better for the submission write-up.</pros>
      <cons>Longer to type in every verification command and in the demo video.</cons>
    </option>
    <option id="other">
      <name>A different label you name</name>
      <pros>Whatever fits how you want the URL to read, including reusing a subdomain you already have a use for.</pros>
      <cons>None, provided it is under the vockell.com zone and does not collide with an existing record.</cons>
    </option>
  </options>
  <resume-signal>Reply with the label: prodfin, productionfinance, or the exact hostname you want</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the host facts and write the deploy runbook</name>
  <files>deploy/hosting.env, deploy/README.md</files>
  <read_first>
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-RESEARCH.md` § "SHP-03" — the verbatim `dig` and `whois` output establishing Register.com as the zone host, the confirmed apex A record, and the practical consequence that record creation is a manual registrar-panel step
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-RESEARCH.md` § "Architecture Patterns" — the Track B topology diagram showing where each variable in `hosting.env` is consumed
    - `.claude/CLAUDE.md` § Constraints — the instance name `vockell_dot_com_LAMP`, AWS profile `newaccount`, region `us-west-2`, static IP `35.165.60.123`, SSH user `bitnami`, and the key filename
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-CONTEXT.md` — D-15 through D-23, the whole Track B decision block this runbook records
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-01-SUMMARY.md` — the relocated SSH key path, which `hosting.env` must reference
    - `.gitignore` — confirm that a file named `hosting.env` under `deploy/` is not caught by the existing `.env` and `.env.*` rules
  </read_first>
  <action>
Write the two files every later Track B task reads, so no downstream task has to re-derive a hostname or a path.

Create `deploy/hosting.env` as a plain shell-sourceable file — `KEY=value` lines, no export keyword needed, comments with a leading hash. Record: `PRODFIN_HOST` set to the hostname confirmed at Task 1; `PRODFIN_STATIC_IP` set to `35.165.60.123`; `PRODFIN_INSTANCE` set to `vockell_dot_com_LAMP`, noting it will change after the resize in plan 01-07; `PRODFIN_AWS_PROFILE` set to `newaccount`; `PRODFIN_AWS_REGION` set to `us-west-2`; `PRODFIN_SSH_USER` set to `bitnami`; `PRODFIN_SSH_KEY` set to `~/.ssh/LightsailDefaultKey-us-west-2.pem` (the destination plan 01-01 relocated the key to); `PRODFIN_APP_PORT` set to `8000`; `PRODFIN_APP_ROOT` set to `/opt/prodfin`; `PRODFIN_SERVICE` set to `prodfin.service`; and `PRODFIN_SERVICE_USER` set to `prodfin`.

Confirm before writing that the existing `.gitignore` rules for `.env` and `.env.*` do not catch a file named `hosting.env` under `deploy/` — they should not, since one matches a file literally named `.env` and the other matches a dot-env with a suffix. This file is intended to be tracked and public: it contains a hostname, a public IP, an instance name, a port and a path, and no secret of any kind. Add a header comment saying so explicitly, so nobody later adds a credential to it out of convenience.

Create `deploy/README.md` as the Track B runbook. Record, in order: the topology, restated from 01-RESEARCH.md's diagram — Register.com DNS resolves the subdomain to the static IP; Bitnami Apache holds ports 80 and 443 and reverse-proxies to uvicorn on localhost; systemd supervises the uvicorn process; the isolated `uv`-managed Python 3.12 never touches the system Python 3.9.2 that Bitnami and Apache depend on. Then the DNS facts from the live lookup: the zone is at Register.com with nameservers dns105 and dns106.register.com, the registrar is Register.com - Network Solutions, LLC, the apex already resolves to the static IP, and there is no Route 53 hosted zone for this domain, so no AWS DNS command applies. Then the exact A-record specification to create: record type A, host the confirmed label, value `35.165.60.123`, TTL whatever the panel's default is (a low TTL is preferable but not required). Then the D-21 ordering with a one-line reason for each step, and the rollback position at each: what is recoverable and how. Then the standing rule that vockell.com is a live personal site sharing this box, and that no step in Track B may modify an existing DNS record, an existing vhost, or an existing certificate binding except where a task says so explicitly and takes a snapshot first.

Add a short "How to verify" section listing the exact commands each later plan uses, so they are in one place: `dig +short A $PRODFIN_HOST`, `curl -I https://vockell.com`, the SSH invocation, `systemctl is-active`, and the external HTTPS probe.
  </action>
  <verify>
    <automated>set -a && . ./deploy/hosting.env && set +a && test -n "$PRODFIN_HOST" && test "$PRODFIN_STATIC_IP" = "35.165.60.123" && test "$PRODFIN_APP_PORT" = "8000" && test -f "$(eval echo $PRODFIN_SSH_KEY)" && git check-ignore -q deploy/hosting.env; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/hosting.env` exists and sourcing it sets `PRODFIN_HOST`, `PRODFIN_STATIC_IP`, `PRODFIN_INSTANCE`, `PRODFIN_AWS_PROFILE`, `PRODFIN_AWS_REGION`, `PRODFIN_SSH_USER`, `PRODFIN_SSH_KEY`, `PRODFIN_APP_PORT`, `PRODFIN_APP_ROOT`, `PRODFIN_SERVICE` and `PRODFIN_SERVICE_USER`
    - `PRODFIN_HOST` equals the label confirmed at Task 1, fully qualified under `vockell.com`
    - `git check-ignore deploy/hosting.env` finds no matching ignore rule — the file is trackable
    - `deploy/hosting.env` contains no value that looks like a credential: no line whose value is longer than 60 characters, and no occurrence of the words key, secret, token or password on the right-hand side of an assignment other than the `PRODFIN_SSH_KEY` path
    - The file at `$PRODFIN_SSH_KEY` exists on the developer machine
    - `deploy/README.md` contains sections for the topology, the DNS facts, the A-record specification, the D-21 ordering with rollback positions, and How to verify
    - `deploy/README.md` states that the zone is at Register.com and that no Route 53 command applies
  </acceptance_criteria>
  <done>Every host fact a later Track B task needs is in one shell-sourceable file, and the runbook records the topology, the DNS facts and the ordering with rollback positions.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Create the subdomain A record in the Register.com DNS panel</name>
  <action>Create the subdomain A record in the Register.com DNS panel</action>
  <instructions>
    This is the one step in Track B that cannot be automated. 01-RESEARCH.md § SHP-03 confirmed by live `dig` and `whois` that the vockell.com zone is hosted at Register.com (Network Solutions), not in Route 53, and found no confirmed public API for this account. Creating the record is a login to the account panel, which only you can do.

    Everything automatable has already been done: the hostname is confirmed and recorded in `deploy/hosting.env`, the record specification is written into `deploy/README.md`, and the verification command is ready to run the moment you say the record is in.

    What to create, exactly one new record:
    - Record type: **A**
    - Host / name: the label from `PRODFIN_HOST` in `deploy/hosting.env` (the part before `.vockell.com`)
    - Points to / value: **35.165.60.123**
    - TTL: the panel default is fine; a lower TTL propagates faster if the panel offers a choice

    Do not modify, replace or delete any existing record. The apex `vockell.com` A record already points at the same address and must be left exactly as it is — it serves your live site. You are adding one sibling record, nothing else.

    If the panel refuses the label because a record with that name already exists, stop and say so rather than overwriting it.
  </instructions>
  <verification>
    Claude will run `dig +short A $PRODFIN_HOST` and require it to return exactly `35.165.60.123` and nothing else, then re-run `dig +short A vockell.com` and require the apex to still return the same address — confirming the addition did not disturb it. Propagation can take from a few minutes to a few hours depending on the panel's TTL, so Claude will retry on an empty result rather than reading empty as success.
  </verification>
  <resume-signal>Type "done" when the A record is saved in the panel, or describe what the panel would not let you do</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public DNS → anonymous visitor | The zone answer is what sends a judge's browser at the box; a wrong answer sends them somewhere else entirely |
| Register.com account panel → the vockell.com zone | Zone control lives with a third-party registrar account, outside both the repository and the AWS account |
| `deploy/hosting.env` → public repository | Everything in this file becomes public and is read by every later deploy step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-29 | Spoofing | the new subdomain A record | high | mitigate | The record value is pinned to `35.165.60.123`, the static IP already bound to the instance and already serving the apex. No CNAME to a third party. Verification asserts the resolved set is exactly that one address rather than merely containing it. |
| T-01-30 | Denial of Service | editing the wrong record and breaking the vockell.com apex | high | mitigate | The checkpoint instructions state the change is the addition of one sibling record and that no existing record may be modified; verification re-queries the apex after the change and requires it unchanged. Recorded as a prohibition in `must_haves.prohibitions`. |
| T-01-31 | Elevation of Privilege | Register.com account access | medium | transfer | Zone control is the registrar's and account security including multi-factor authentication is the account owner's responsibility. Recorded here rather than managed by this project; noted in `deploy/README.md`. |
| T-01-32 | Information Disclosure | `deploy/hosting.env` in a public repository | medium | mitigate | The file carries only a hostname, a public IP, an instance name, a port and a key *path* — never key material. A header comment states the rule and the acceptance criteria assert no long or credential-shaped value is present. |
| T-01-33 | Information Disclosure | publishing the subdomain before the host is hardened | low | accept | The subdomain resolves to a box already publicly serving vockell.com on the same address, so the record adds no new exposure. Accepted at ASVS L1; the app itself is not reachable until plan 01-09 adds the vhost. |
| T-01-SC | Tampering | package-manager installs | high | mitigate | No package installs in this plan. The Phase 1 dependency audit lives in 01-RESEARCH.md § "Package Legitimacy Audit" — all Approved, no `[SLOP]`, no genuine risk signal. |
</threat_model>

<verification>
One-time probes, run once after the record is created. These are not CI tests: GitHub runners have no visibility into this zone's propagation and D-19 rules out CI-driven deploy.

1. `dig +short A $PRODFIN_HOST` returns exactly one line, `35.165.60.123`. Retry on empty output — empty means not yet propagated, never success. Record the number of retries and the elapsed time in the SUMMARY.
2. `dig +short A vockell.com` still returns `35.165.60.123` — the apex is untouched.
3. `dig +short NS vockell.com` still returns the two register.com nameservers — the zone host is unchanged.
4. `curl -I https://vockell.com` returns 200 — the live site is unaffected by the zone edit.

**Flagged planner assumption:** `deploy/README.md` records the exact A-record specification, but the Register.com panel's field labels are not known from this side and may differ from "Host" and "Points to". If the panel's terminology differs, the record content is unchanged — one A record, the label, the IP. This is recorded so a labelling mismatch is read as a UI difference rather than a specification error.
</verification>

<success_criteria>
- The subdomain label is confirmed by the developer, not assumed (D-14).
- `dig +short A <subdomain>` resolves to the Lightsail static IP (SHP-03).
- The vockell.com apex record and the zone's nameservers are unchanged, and the live site still serves.
- `deploy/hosting.env` and `deploy/README.md` carry every host fact and the D-21 ordering that plans 01-07 through 01-09 read.
- The DNS clock started before the resize, per D-21.
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by this plan:

| Symbol | Kind | Location |
|--------|------|----------|
| `deploy/` | directory | new |
| `deploy/hosting.env` | file | new |
| `deploy/README.md` | file | new |
| `PRODFIN_HOST` | shell variable / config key | `deploy/hosting.env` |
| `PRODFIN_STATIC_IP` | shell variable | `deploy/hosting.env` |
| `PRODFIN_INSTANCE` | shell variable | `deploy/hosting.env` |
| `PRODFIN_AWS_PROFILE` | shell variable | `deploy/hosting.env` |
| `PRODFIN_AWS_REGION` | shell variable | `deploy/hosting.env` |
| `PRODFIN_SSH_USER` | shell variable | `deploy/hosting.env` |
| `PRODFIN_SSH_KEY` | shell variable | `deploy/hosting.env` |
| `PRODFIN_APP_ROOT` | shell variable | `deploy/hosting.env` |
| `PRODFIN_SERVICE` | shell variable | `deploy/hosting.env` |
| `PRODFIN_SERVICE_USER` | shell variable | `deploy/hosting.env` |
| the confirmed subdomain hostname | DNS A record | Register.com zone for vockell.com |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-foundations-source-truth-deploy-path/01-06-SUMMARY.md` when done. Record: the confirmed hostname, the timestamp the record was created, how long propagation took and how many `dig` retries it needed, the verbatim `dig` output for both the subdomain and the apex, and any difference between the Register.com panel's field labels and the specification in `deploy/README.md`.
</output>
