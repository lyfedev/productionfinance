---
phase: 01-foundations-source-truth-deploy-path
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - .planning/SOURCE-TRUTH.md
  - sources/MANIFEST.yaml
  - sources/ny/2026-08-24-ny-enacted-budget-film-credit-extract.pdf
  - sources/ny/2026-08-24-esd-film-credit-guidelines.pdf
  - sources/ny/2026-08-24-esd-film-prod-cpa-aup.pdf
  - sources/ga/2026-08-24-dor-film-tax-credit-resources.html
  - sources/ga/2026-08-24-dor-film-tax-credit-withholding-instructions.html
  - tests/test_source_truth.py
autonomous: true
requirements: [SRC-01, SRC-02, SRC-04, SRC-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/SOURCE-TRUTH.md` has one entry per source-verification question (SRC-01, SRC-02, SRC-04, SRC-05), each carrying the question, the answer, a primary-source URL, date_checked, a confidence tier, and what was refuted (D-11)"
    - "The New York cap entry states the base-program annual cap as an exact figure with its own primary-source URL, and separately states the independent-film pool figure and its two sub-pools — the three numbers are never summed into a single reported cap"
    - "When the enacted-bill cross-check does not close the 700M-versus-800M inconsistency, the entry carries an explicit unresolved-conflict record naming both figures and both documents, and no single figure is recorded as the answer (D-13)"
    - "Every figure quoted in the New York cap entry is transcribed from a raw text extraction of an archived document, never from an LLM summary of it"
    - "Cap figures are recorded in the exact numeric form and unit the source uses, with the source's own wording preserved alongside — never rounded, rescaled, or reduced to a bare integer with the wording dropped"
    - "The Connecticut entry records the seven CSV column headers verbatim in their published order, plus the six data-quality artifacts the research found by direct inspection"
    - "Re-running the Connecticut archival step produces either a byte-identical archived file with the manifest unchanged, or a new dated archive file plus a new manifest entry — it never overwrites an existing archived document in place"
    - "The Georgia entry records the full dated loan-out withholding schedule so a rate lookup one day either side of a schedule boundary resolves to the correct band"
    - "Each Georgia rate is recorded as the exact decimal string the source prints, never as a rounded or float-converted value"
    - "A refuted or refined working hypothesis is recorded rather than deleted — both the SRC-01 and SRC-05 hypotheses appear in their entries with what the evidence did to them (D-12)"
    - statement: "Re-verifying the partner track at submission-filing time appends a dated re-check line to the SRC-04 entry rather than replacing the original 2026-08-24 owner confirmation"
      verification: backstop
    - "`uv run pytest tests/test_source_truth.py` exits 0 and fails when any entry is missing a URL, a date_checked or a confidence tier"
    - "Every `source_document` referenced by a fixture or a SOURCE-TRUTH entry resolves to a `sources/MANIFEST.yaml` row whose recorded sha256 matches the file on disk"
  artifacts:
    - path: ".planning/SOURCE-TRUTH.md"
      provides: "The answers to SRC-01, SRC-02, SRC-04 and SRC-05, with the reconciliation reasoning Phase 2's rule files cite alongside the primary URL (D-11)"
      contains: "date_checked"
    - path: "tests/test_source_truth.py"
      provides: "Structural assertions over SOURCE-TRUTH.md entries and the manifest reconciliation check"
      contains: "sha256"
    - path: "sources/ga/2026-08-24-dor-film-tax-credit-resources.html"
      provides: "Byte-for-byte archive of the Georgia DOR page carrying the withholding rate schedule"
  key_links:
    - from: ".planning/SOURCE-TRUTH.md"
      to: "sources/MANIFEST.yaml"
      via: "each entry names the archived document its answer was transcribed from"
      pattern: "sources/"
    - from: "tests/test_source_truth.py"
      to: "sources/MANIFEST.yaml"
      via: "re-hashes every manifest path and compares against the recorded sha256"
      pattern: "sha256"
    - from: "tests/test_source_truth.py"
      to: ".planning/SOURCE-TRUTH.md"
      via: "parses the entry headings and asserts each carries the six required elements"
      pattern: "confidence"
  prohibitions:
    - requirement_id: "SRC-01"
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT resolve a genuine conflict between two authoritative sources by picking whichever figure is more convenient to encode — where the evidence does not close, the record is an explicit unresolved conflict naming both figures and both documents, and the conflict stays visible downstream rather than being flattened into a single number"
    - requirement_id: "SRC-05"
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT record a figure at a confidence tier the evidence does not support — a rate read from a page that does not explicitly name the thing being asked about is recorded with that caveat stated, not promoted to a clean answer because it is contextually probable"
---

<objective>
Write the answers to the four source-verification questions into a single reviewable record, with the New York cap conflict run down against the statute rather than waved away.

Purpose: Phase 2's engine encodes these figures. A wrong constant here propagates into every downstream number, and the product's whole claim is correctness. The research already answered SRC-02 and SRC-05 by direct fetch and confirmed the SRC-04 partner track, but it deliberately left SRC-01 open: three official New York documents disagree, and closing that needs the enacted budget bill text, which is the top of the source-precedence ordering and which neither ESD PDF is.

Output: `.planning/SOURCE-TRUTH.md` with four entries, the supporting documents archived and hashed, and a test that fails when an entry is missing its provenance or when the manifest and the files on disk have drifted apart.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-source-truth-deploy-path/01-CONTEXT.md
@.planning/phases/01-foundations-source-truth-deploy-path/01-RESEARCH.md
@.planning/phases/01-foundations-source-truth-deploy-path/01-04-SUMMARY.md
@.planning/research/PITFALLS.md
@sources/MANIFEST.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: SRC-01 — run the New York cap conflict down against the enacted budget bill</name>
  <files>.planning/SOURCE-TRUTH.md, sources/MANIFEST.yaml, sources/ny/2026-08-24-ny-enacted-budget-film-credit-extract.pdf, sources/ny/2026-08-24-esd-film-credit-guidelines.pdf, sources/ny/2026-08-24-esd-film-prod-cpa-aup.pdf</files>
  <read_first>
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-RESEARCH.md` § "SRC-01" in full — the four documents read directly, their verbatim quoted sentences, the two competing explanations for the anomaly, and the explicit instruction that this must be resolved against the enacted budget bill before SOURCE-TRUTH.md is finalized
    - `.planning/research/PITFALLS.md` §D1 — the original framing of the 700M/800M question, and §D2/§D3 — the source-precedence ordering to apply and the requirement to display an unresolved conflict rather than silently picking a value
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-CONTEXT.md` — D-11 (entry shape), D-12 (a refuted hypothesis is recorded, not deleted), D-13 (unresolved conflicts are recorded explicitly)
    - `.planning/REQUIREMENTS.md` line for SRC-01 — the working hypothesis stated verbatim, which this entry confirms, refines or refutes
    - `sources/MANIFEST.yaml` — the entry shape any newly archived document follows
  </read_first>
  <action>
Close, or honestly fail to close, the three-document New York cap inconsistency the research surfaced.

The state of play: the April 2025 ESD Guidelines PDF and the current live esd.ny.gov programme page both state 700 million per year for the base Film Production and Post-Production Tax Credit Program. A May 2026 ESD Agreed-Upon-Procedures PDF states 800 million using the same sentence construction for what reads as that same base programme. Separately, the Independent Film Production Tax Credit Program is confirmed at roughly 100 million annually, split into a ~20 million pool for productions with 10 million or less in qualified costs and a ~80 million pool above that. Press coverage explains 800 million as the combined total. The research judged the press explanation directionally right but insufficient, because the AUP document is official and more recently dated, and PITFALLS §D2's precedence ordering puts recency above press summary.

Go to the statute. Search nysenate.gov and assembly.state.ny.us for the enacted New York State budget bill text carrying the Amount of Credits Available language for the film tax credit — the FY2026 budget approved around May 2025, and if the May 2026 AUP date suggests a later action, also an FY2027 budget that would have been approved around April 2026. The enacted bill text is the top of the precedence ordering; neither ESD PDF is the statute.

Archive whatever primary documents you read into `sources/ny/` per D-08, each with a manifest entry: the enacted bill extract if you obtain it, plus the two ESD PDFs the research read (the Guidelines PDF and the AUP PDF) so the conflict itself is reproducible from committed bytes rather than only from a research narrative. Use raw text extraction — `pdftotext -layout` for PDFs, direct fetch for HTML — never an LLM summary as the source of a digit.

Write the SRC-01 entry in `.planning/SOURCE-TRUTH.md` per D-11: the question verbatim from REQUIREMENTS.md; the answer; the primary-source URL; `date_checked`; a confidence tier; and a "what was refuted or refined" section. Record the working hypothesis from REQUIREMENTS.md and state precisely what the evidence did to it (D-12) — the research already found it directionally correct but not fully sufficient, and that refinement is itself the finding worth keeping.

Report the base-programme cap and the independent-film pool as separate figures with separate sources. Do not report a single summed cap: the two are different programmes with different eligibility, and Phase 2's rule files need them separable. State each figure in the numeric form and unit the source itself uses, quoting the source's wording alongside the number — a bare integer with the wording dropped loses the distinction between "700 million per year" and "700 million allocated through 2036".

If the enacted bill text closes the question, record the closed answer with the bill citation and note that the AUP document's 800 million figure is superseded or was imprecise, whichever the statute shows. If it does not close, apply D-13: write an explicit unresolved-conflict record naming both figures, both documents with their dates, both competing explanations, and what evidence would settle it. Do not pick one. PRV-07 in Phase 8 surfaces conflicting authoritative sources as unresolved conflicts rather than silently resolving them, and this is the first one.
  </action>
  <verify>
    <automated>uv run python -c "import re,sys; t=open('.planning/SOURCE-TRUTH.md').read(); e=t.split('SRC-01',1)[1].split('SRC-0',1)[0]; assert 'date_checked' in e or 'Date checked' in e; assert 'http' in e; assert re.search(r'700', e) and re.search(r'800', e); assert 'refut' in e.lower() or 'refine' in e.lower(); print('SRC-01 entry shape ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/SOURCE-TRUTH.md` exists and contains a heading naming SRC-01
    - The SRC-01 entry contains the question, an answer, at least one URL, a `date_checked` value, a confidence tier, and a section recording what was refuted or refined
    - The entry states the base-programme cap and the independent-film pool as two separate figures, each with its own source URL, and states no single summed figure as "the cap"
    - The entry names both the 700 million and the 800 million figures and the specific documents each comes from with their dates
    - Either the entry cites enacted bill text closing the question, or it carries an explicit unresolved-conflict record naming both explanations and the evidence that would settle it
    - Every document the entry cites as primary has a row in `sources/MANIFEST.yaml` whose recorded sha256 matches the archived file on disk
    - The SUMMARY records whether the conflict closed and, if not, exactly what was searched and what was not found
  </acceptance_criteria>
  <done>The New York cap question has a written answer sourced to the statute, or an explicit unresolved-conflict record — and either way the supporting documents are archived in-repo with matching hashes.</done>
</task>

<task type="auto">
  <name>Task 2: SRC-02 and SRC-05 — Connecticut CSV schema and the Georgia withholding schedule</name>
  <files>.planning/SOURCE-TRUTH.md, sources/MANIFEST.yaml, sources/ga/2026-08-24-dor-film-tax-credit-resources.html, sources/ga/2026-08-24-dor-film-tax-credit-withholding-instructions.html</files>
  <read_first>
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-RESEARCH.md` § "SRC-02" — the verbatim seven-column header row, the endpoint URL, and the six data-quality gotchas found by direct inspection
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-RESEARCH.md` § "SRC-05" — the verbatim five-row withholding schedule, and the MEDIUM-confidence caveat that the captured page fragment does not explicitly tie the table to loan-out withholding
    - `.planning/research/PITFALLS.md` §A1.10 — the original 4.99% versus 5.75% framing this entry resolves
    - `.planning/REQUIREMENTS.md` lines for SRC-02 and SRC-05 — the questions verbatim and the SRC-05 working hypothesis
    - `sources/ct/` and `sources/MANIFEST.yaml` — the Connecticut export plan 01-04 already archived, which this entry cites rather than re-fetching
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-CONTEXT.md` — D-11, D-12, D-13
  </read_first>
  <action>
Write the two entries the research already answered, and close the one caveat it left open.

For SRC-02, cite the Connecticut export plan 01-04 already archived rather than re-fetching. If a re-fetch is needed for any reason, archive it as a new dated file with its own manifest entry — never overwrite the existing archived document in place, because the existing fixture cites that file's hash and rewriting it would silently break the citation.

Write the SRC-02 entry per D-11. Record the exact seven-column header row verbatim in its published order: Production Company, Qualified CT Expenditures, Date Issued, Amount of Tax Credit Issued, Program Name, Statutory Reference, Municipality. Record the endpoint URL and the dataset landing page. Record all six data-quality artifacts the research found, because each one is a bug waiting to happen in Phase 5's ingestion: the blank row immediately following the header; money as quoted text with a currency symbol and thousands commas; an observed trailing-period formatting artifact; Municipality blank on some rows, so the field must be nullable; Date Issued carrying an ISO datetime with a time component despite being a calendar-date event; and Program Name mixing three distinct statutory programmes in one file, so ingestion must filter on it. State explicitly that the schema implies issued-stage disclosure — the columns are Date Issued and Amount of Tax Credit Issued — which is what makes Connecticut a strong validation source and what plan 01-04's fixture relies on.

For SRC-05, archive the Georgia DOR film-tax-credit-resources page into `sources/ga/` with a manifest entry, and also fetch and archive the specifically-linked Film Tax Credit Withholding Instructions and Forms page. The research flagged that the captured fragment presents the rate table under a generic Withholding Rate heading without an explicit sentence naming loan-out withholding, and recommended opening that second page to get the explicit statement before finalizing the entry. Do that.

Write the SRC-05 entry per D-11 recording the full dated schedule exactly as the source prints it: 4.99% from January 1 2026 to current; 5.19% for calendar 2025; 5.39% for calendar 2024; 5.49% for calendar 2023; 5.75% for December 31 2022 and prior. Record each rate as the exact decimal string the page prints — do not round, do not convert to a float, and do not collapse the schedule to a single current rate, because a production's terms lock at application and a lookup one day either side of a boundary must resolve to the correct band.

Apply D-12 to the SRC-05 working hypothesis. REQUIREMENTS.md states it as "5.75% is pre-2024-reform, 4.99% is current". The research found this directionally correct but imprecise: it is not a single pre/post step but a five-year declining schedule with three intermediate values nobody had recorded. Write that refinement into the entry as a finding rather than quietly replacing the hypothesis with the answer.

State the confidence tier honestly on both. SRC-02 is HIGH — a direct read of the live endpoint. SRC-05's rate figures are HIGH, but if the second Georgia page does not produce an explicit sentence tying the schedule to loan-out withholding, the loan-out-specificity claim stays MEDIUM with that caveat written into the entry. Do not promote it to a clean answer because it is contextually probable.
  </action>
  <verify>
    <automated>uv run python -c "import sys; t=open('.planning/SOURCE-TRUTH.md').read(); assert 'SRC-02' in t and 'SRC-05' in t; assert 'Qualified CT Expenditures' in t and 'Statutory Reference' in t and 'Municipality' in t; assert '4.99' in t and '5.19' in t and '5.39' in t and '5.49' in t and '5.75' in t; print('SRC-02/05 entries ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The SRC-02 entry contains all seven Connecticut column names verbatim, in published order
    - The SRC-02 entry names all six data-quality artifacts and states that the schema implies issued-stage disclosure
    - The SRC-05 entry contains all five rate values as exact decimal strings with their date bands
    - The SRC-05 entry states the confidence tier for the loan-out-specificity claim separately from the tier for the rate figures
    - Both entries carry a question, an answer, a primary-source URL, a `date_checked`, a confidence tier and a what-was-refuted-or-refined section
    - The Georgia pages are archived under `sources/ga/` with manifest entries whose sha256 matches the files on disk
    - `sources/ct/` still contains exactly the file plan 01-04 archived, with its original sha256 unchanged in the manifest
  </acceptance_criteria>
  <done>The Connecticut schema and the Georgia withholding schedule are recorded with their full detail, their artifacts and their honest confidence tiers, sourced to archived bytes.</done>
</task>

<task type="auto">
  <name>Task 3: SRC-04 entry, the SOURCE-TRUTH shape test, and manifest reconciliation</name>
  <files>.planning/SOURCE-TRUTH.md, tests/test_source_truth.py</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the SRC-04 line, already marked complete, with its owner-confirmation date and its re-verify-at-filing-time note
    - `.planning/STATE.md` § "Blockers/Concerns" — the resolved SRC-04 entry recording Parallel as owner-confirmed 2026-08-24
    - `.planning/phases/01-foundations-source-truth-deploy-path/01-CONTEXT.md` — D-11 (entry shape), and the statement that SRC-04 needs no Phase 1 integration
    - `.planning/SOURCE-TRUTH.md` — the three entries written by Tasks 1 and 2, whose shape this test asserts
    - `sources/MANIFEST.yaml` and `tests/test_validation_pair_fixtures.py` — the manifest structure and the existing test conventions this module follows
  </read_first>
  <action>
Record the partner track, then make the whole source-truth record structurally checkable.

Write the SRC-04 entry per D-11. The question is the partner track; the answer is Parallel, owner-confirmed on 2026-08-24 before planning began; the source is the owner's confirmation rather than a public URL, and the entry should say so plainly rather than inventing a citation. Record that Parallel's Search API must be called at runtime via the official `parallel-web` SDK, that this makes SHP-06 unconditional and load-bearing in Phase 7, and that Phase 1 deliberately carries no `parallel-web`, `google-genai` or `google-adk` runtime dependency — those land in the phases that exercise them. Record explicitly that the "Our track: IBM" line in `hackathon-brief.md` refers to the sibling animatic project and does not apply here, so a later reader does not re-open a settled question.

Add a `Re-verification log` subsection to the SRC-04 entry with a dated line for the original confirmation. State that the submission-portal re-check at filing time appends a new dated line here rather than replacing the original — the original confirmation is the record of what was believed when, and overwriting it would erase that. Phase 8 performs the re-check; this plan only establishes the shape.

Write `tests/test_source_truth.py` with three groups of assertions.

First, entry shape: parse `.planning/SOURCE-TRUTH.md` into per-requirement sections keyed by the SRC identifier in the heading. Assert every requirement id in the set SRC-01, SRC-02, SRC-04, SRC-05 has exactly one section. For each, assert the section contains a question, a non-empty answer, a `date_checked` value that parses as a date, and a confidence tier drawn from a fixed vocabulary. For SRC-01, SRC-02 and SRC-05, additionally assert the section contains at least one URL — SRC-04 is exempt because its source is an owner confirmation, and the test should carry a comment saying so rather than silently special-casing it.

Second, manifest reconciliation, which is the assertion that makes the whole audit trail structurally true rather than aspirational: for every entry in `sources/MANIFEST.yaml`, assert the file at `path` exists and that its computed sha256 equals the recorded value. Then assert the reverse direction too — every file under `sources/` other than `MANIFEST.yaml` itself has a manifest row — so an archived document cannot sit in the repository uncited and unhashed.

Third, cross-reference: for every fixture in `tests/fixtures/validation_pairs/` with a non-null `source_document`, assert that path appears as a manifest `path`. This is what stops a fixture from citing a document that was never archived.

Add a deliberate non-vacuity proof to the acceptance criteria rather than to the committed test: temporarily append a byte to one archived source document, confirm the reconciliation test goes red, then restore the file. A hash check that has never been observed failing has not been shown to check anything.
  </action>
  <verify>
    <automated>uv run pytest tests/test_source_truth.py -q && uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/SOURCE-TRUTH.md` contains exactly four requirement sections: SRC-01, SRC-02, SRC-04 and SRC-05
    - The SRC-04 section names Parallel, carries the 2026-08-24 owner-confirmation date, states that Phase 1 adds no partner or Google SDK runtime dependency, and contains a `Re-verification log` subsection with one dated line
    - `tests/test_source_truth.py` exists and `uv run pytest tests/test_source_truth.py -q` exits 0
    - The test asserts every manifest `path` exists and hashes to its recorded `sha256`, and asserts every file under `sources/` other than the manifest has a manifest row
    - The test asserts every fixture `source_document` resolves to a manifest `path`
    - Appending a byte to one archived source document makes `tests/test_source_truth.py` fail; record the observed failure output in the SUMMARY, then restore the file and confirm the suite is green again
    - `uv run pytest tests/ -q` exits 0 across the whole suite
  </acceptance_criteria>
  <done>All four source-verification questions have entries with full provenance, and a test proves the archive, the manifest and the fixtures all agree — and has been observed failing when they do not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nysenate.gov / dor.georgia.gov / data.ct.gov → local archive | Untrusted bytes fetched over the network become the project's cited statutory record |
| archived document → SOURCE-TRUTH.md answer | The transcription step where an official-looking wrong number enters Phase 2's rule files |
| SOURCE-TRUTH.md → Phase 2 rule files | Every constant the engine encodes traces to an entry here; a wrong one propagates into every downstream figure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23 | Tampering | archived statutory and agency documents under `sources/` | high | mitigate | `tests/test_source_truth.py` re-hashes every manifest path on every run and additionally asserts no archived file sits uncited; the acceptance criteria require observing the check go red against a mutated file. |
| T-01-24 | Spoofing | a secondary or press source cited as if primary | high | mitigate | Entries record a confidence tier and a source URL, and the SRC-01 entry is required to cite enacted bill text or state explicitly that it could not. Press corroboration is recorded as such, never as the primary figure. |
| T-01-25 | Repudiation | a conflict silently resolved to one figure | high | mitigate | D-13 is implemented as a required unresolved-conflict record naming both figures and both documents; the acceptance criteria assert both the 700 and 800 figures appear in the entry with their source documents. Recorded as a prohibition in `must_haves.prohibitions`. |
| T-01-26 | Tampering | overwriting an already-cited archived document on re-fetch | medium | mitigate | Re-fetch produces a new dated archive plus a new manifest entry; the acceptance criteria assert the Connecticut file archived by plan 01-04 retains its original sha256. |
| T-01-27 | Information Disclosure | `.planning/SOURCE-TRUTH.md` and `sources/` published | low | accept | Public government material and the project's own reasoning; publishing both is the audit trail PROJECT.md commits to. |
| T-01-28 | Spoofing | a confidence tier stated higher than the evidence supports | medium | mitigate | SRC-05's loan-out-specificity claim carries its own separate tier from the rate figures, and the second Georgia page is fetched specifically to try to raise it. Recorded as a prohibition. |
| T-01-SC | Tampering | package-manager installs | high | mitigate | No new installs. The Phase 1 dependency set is covered by the audit table in 01-RESEARCH.md § "Package Legitimacy Audit" — all Approved, no `[SLOP]` — and `lockfile-scan` guards the resolved set on every commit. |
</threat_model>

<verification>
Run after all three tasks:

1. `uv run pytest tests/ -q` — whole suite green.
2. Append a byte to one archived source document, run `uv run pytest tests/test_source_truth.py -q`, observe a hard failure, restore, re-run green. Record both outputs.
3. Read `.planning/SOURCE-TRUTH.md` end to end by eye. For each entry, confirm a reader who has never seen the research document can tell what was asked, what the answer is, where it came from, when it was checked, how confident it is, and what was refuted.
4. Confirm the SRC-01 entry does not state a single summed cap figure anywhere, and that if the conflict did not close, both figures and both documents are named.

Manual document review is the correct instrument for SRC-01, SRC-02 and SRC-05 per 01-VALIDATION.md — whether a source is genuinely primary and whether a conflict is genuinely closed are judgments, not runtime assertions. The structural half is automated in `tests/test_source_truth.py`.

**Flagged planner assumption (spec-less edge probe, unresolved for SRC-04):** the probe asked a concurrency question of SRC-04 — what is guaranteed if the check is interrupted or run in parallel. SRC-04's outstanding work is a re-verify at submission-filing time in Phase 8, which this phase cannot observe. The predicate is therefore recorded as a `backstop` truth: the re-verification log appends rather than replaces. At verify time, a verifier that cannot confirm it with explicit evidence must abstain rather than pass it silently.
</verification>

<success_criteria>
- SRC-01, SRC-02, SRC-04 and SRC-05 each have a written answer carrying a primary-source URL and the date it was checked (ROADMAP Phase 1 success criterion 1).
- The New York cap question is closed against the statute or recorded as an explicit unresolved conflict (SRC-01, D-13).
- The Connecticut column headers are recorded verbatim with their six data-quality artifacts (SRC-02).
- The Georgia loan-out withholding schedule is recorded in full with an honest confidence tier on the loan-out-specificity claim (SRC-05).
- Both stated working hypotheses appear in their entries with what the evidence did to them (D-12).
- The archive, the manifest and the fixtures are proven to agree, and the proof has been observed failing (D-10).
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by this plan:

| Symbol | Kind | Location |
|--------|------|----------|
| `.planning/SOURCE-TRUTH.md` | file | new |
| `Re-verification log` | document subsection heading | `.planning/SOURCE-TRUTH.md` SRC-04 entry |
| `sources/ga/` | directory | new |
| `tests/test_source_truth.py` | pytest module | new file |
| `test_every_entry_has_provenance` | test function | `tests/test_source_truth.py` |
| `test_manifest_hashes_match_files_on_disk` | test function | `tests/test_source_truth.py` |
| `test_every_archived_file_has_a_manifest_row` | test function | `tests/test_source_truth.py` |
| `test_fixture_source_documents_resolve_to_manifest` | test function | `tests/test_source_truth.py` |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-foundations-source-truth-deploy-path/01-05-SUMMARY.md` when done. Record: whether the New York cap conflict closed and against which bill, or exactly what was searched and not found; the sha256 of every newly archived document; whether the second Georgia page produced an explicit loan-out sentence and what confidence tier that settled on; and the observed failure output from the mutated-document hash check.
</output>
