---
phase: 02-engine-spine-incentive-interpreter
plan: 09
type: execute
wave: 2
depends_on: ["02-07", "02-08"]
gap_closure: true
files_modified:
  - engine/credit.py
  - tests/test_engine_credit.py
  - tests/test_engine_against_validation_pairs.py
autonomous: true
requirements: [INC-01, INC-02, INC-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A production date falling exactly on a loan-out withholding band's declared `effective_to` selects THAT band, not the next one — the closed-closed convention for dated ranges is deliberate, commented as such, and proven at the adjacency point (INC-02 adjacency edge)"
    - "The committed Georgia-style schedule's abutting bands still resolve correctly: 2025-12-31 selects the 5.19% band and 2026-01-01 selects the 4.99% band (INC-02 boundary edge)"
    - "Two schedule entries whose date ranges overlap raise, naming both offending bands, instead of silently resolving by declared list order (INC-02)"
    - "The closed-closed dated-range convention is stated in a comment beside the lookup, naming why it differs from the half-open spend-band convention `lookup_flat_rate_by_band` and `_select_audit_fee_tier` use, so the inconsistency is a recorded decision rather than an accident (PRV-03)"
    - "Every active validation pair reproduces its disclosed government figure through `engine.pipeline.price_jurisdiction`, not only through the direct base-then-credit path — Anora exactly Decimal('991190'), Christmas Always exactly Decimal('1159502') (INC-01, INC-06)"
    - "The re-coupled path actually runs `convert_to_net_cash`, so Connecticut's `transferable` mechanism is exercised end to end rather than bypassed — a `NotImplementedError` from any mechanism fails the test rather than being avoided (INC-06)"
    - "The assertion stays on the gross-credit Figure, never on net cash, per RD-03 — routing through `price_jurisdiction` changes which code path produces the figure, never which figure is compared against the disclosure (INC-01)"
    - "The pair count guards still hold: at least three active New York pairs and at least one active Connecticut pair, so a filter silently matching nothing fails loudly rather than reporting a vacuous green (INC-01 empty edge)"
    - statement: "Effective dates are compared as `datetime.date` objects produced by PyYAML and Pydantic, never as strings; no comparison depends on string ordering or on a locale-specific date format"
      verification: backstop
  prohibitions:
    - "MUST NOT convert the loan-out withholding schedule to the half-open convention — the committed Georgia schedule's abutting bands (effective_to 2025-12-31 next to effective_from 2026-01-01) would leave 2025-12-31 matching no band and raising, so a convention flip would silently change a sourced rate lookup"
    - "MUST NOT resolve two overlapping schedule bands by declared list order — an ambiguous rule file raises, because picking a rate with no diagnostic is the silent-fallthrough failure this codebase forbids everywhere else"
    - "MUST NOT assert a disclosed government figure against net cash — the disclosure reports the credit issued, and comparing against net cash would silently require the audit-fee and discount model to be wrong in a compensating direction (RD-03)"
    - "MUST NOT relax an exact-mode validation pair to bounded, or widen a bounded pair's tolerance_bps, to make the re-coupled path pass — a figure that stops reproducing is a finding about the engine, never a fixture edit"
    - "MUST NOT drop or weaken the existing direct base-then-credit assertions when adding the pipeline-routed ones — the two paths agreeing is itself the evidence, so both must run"
    - "MUST NOT use float arithmetic on any monetary, rate or threshold value — stdlib decimal.Decimal only (RD-01)"
    - "MUST NOT import or call any AWS AI service, AWS Textract above all, nor add any of openai, anthropic, langchain*, llama_index or crewai"
  artifacts:
    - path: "engine/credit.py"
      provides: "The documented closed-closed dated-range convention on the loan-out withholding lookup plus an overlap-detection guard"
      contains: "loanout_withholding_schedule"
    - path: "tests/test_engine_against_validation_pairs.py"
      provides: "Every active validation pair re-coupled to price_jurisdiction, exercising base to credit to net cash end to end"
      contains: "price_jurisdiction"
    - path: "tests/test_engine_credit.py"
      provides: "The withholding-schedule adjacency test and the overlapping-bands raise test"
      contains: "effective_to"
  key_links:
    - from: "tests/test_engine_against_validation_pairs.py"
      to: "engine/pipeline.py"
      via: "price_jurisdiction called per pair, with the gross-credit Figure read off the matching PricedProgramme"
      pattern: "price_jurisdiction"
    - from: "engine/credit.py"
      to: "tests/fixtures/jurisdictions/synthetic-ga-style.yaml"
      via: "the five-band dated withholding schedule the adjacency and overlap tests drive the lookup against"
      pattern: "loanout_withholding_schedule"
---

<objective>
Two items dispositioned INCLUDE in `02-07-PLAN.md`'s register, both of which touch code
plan 02-07 owns and therefore run after it.

WR-03: the loan-out withholding schedule lookup uses closed-closed date boundaries while
every other tier lookup in this codebase is half-open, with no test proving the difference
is deliberate and no guard against two bands covering the same date. The convention is
correct for dated ranges and must not be flipped — what is missing is the record and the
guard.

The validation-pairs re-coupling: `tests/test_engine_against_validation_pairs.py`
deliberately bypasses `price_jurisdiction` because Connecticut's `transferable` mechanism
raised `NotImplementedError` before plan 02-04 landed. It has landed. The bypass now
weakens Roadmap SC1's end-to-end evidence — one of the three truths this gap-closure run
exists to restore.

Purpose: INC-02's dated-schedule half, and INC-01 to INC-06 proven as one composition
rather than as two separately-correct halves. Output: the golden-value proof runs through
the real entry point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-engine-spine-incentive-interpreter/02-VERIFICATION.md
@.planning/phases/02-engine-spine-incentive-interpreter/02-REVIEW.md
@.planning/phases/02-engine-spine-incentive-interpreter/02-04-SUMMARY.md
@.planning/phases/02-engine-spine-incentive-interpreter/02-07-PLAN.md
@jurisdictions/SCOPE-FREEZE.md
</context>

<assumption_delta_decision>
**Decision: `no-change`.** The detector's "alongside" signal is a lexical false positive
against ROADMAP SC1's prose about reporting an arrival date next to a value; the full
assessment, including the one genuine identity-model shift found in this gap-closure scope
(the running adjusted base versus the frozen core-expenditure Figure), is recorded once in
`02-07-PLAN.md` and is not restated here.

This plan introduces no new noun. Task 2 does, however, restore an identity that had
temporarily split: "the priced result" and "the golden-value comparison subject" became two
different objects when plan 02-05 decoupled the validation-pairs test from
`price_jurisdiction`. Re-coupling collapses them back to one, which is the whole reason it
is worth doing.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-03 — record the dated-range convention deliberately, and make an ambiguous schedule raise</name>
  <reversibility rating="reversible">A comment, a guard and two tests; the lookup's selection behaviour for every currently-committed schedule is unchanged.</reversibility>
  <precondition>Plan 02-07 is committed and `uv run pytest -q` is green</precondition>
  <files>engine/credit.py, tests/test_engine_credit.py</files>
  <read_first>
    - `engine/credit.py` lines 108-145 — `_select_loanout_rate` in full, including its schedule branch, its `band_desc` construction, its no-matching-band `ValueError` and its scalar fallback
    - `engine/credit.py::lookup_flat_rate_by_band` — the half-open spend-band convention, with its documented `threshold_low <= base` and `base < threshold_high` comparison, this lookup deliberately differs from
    - `engine/net_cash.py::_select_audit_fee_tier` — the second half-open lookup in this codebase, for the same comparison
    - `tests/fixtures/jurisdictions/synthetic-ga-style.yaml`'s `loanout_withholding_schedule` — the five bands copied verbatim from SOURCE-TRUTH.md SRC-05, noting that band boundaries abut (`effective_to` 2025-12-31 beside `effective_from` 2026-01-01) rather than overlap, which is exactly why closed-closed is the correct convention here
    - `tests/test_engine_credit.py::test_per_person_ceiling_loanout_withholding_selects_earlier_band` and `test_per_person_ceiling_unconfirmed_schedule_entry_reports_researched` — the two existing schedule tests that must keep passing
    - `.planning/phases/02-engine-spine-incentive-interpreter/02-REVIEW.md` WR-03 in full, including its two offered fixes — this task takes the first (document the deliberate convention, add the adjacency test) and adds the second's overlap guard on top
    - `engine/models.py::PerPersonCeilingTier` — the `effective_from` / `effective_to` / `loanout_withholding_rate` field types, confirming both dates are `datetime.date` and not strings
  </read_first>
  <behavior>
    - A production date of 2025-12-31 against the Georgia-style schedule selects the 5.19% band, and 2026-01-01 selects the 4.99% band — the abutting boundary resolves to the earlier band on its own last day
    - A production date exactly on a band's `effective_from` selects that band
    - A schedule declaring two bands whose date ranges overlap raises `ValueError` naming both bands, rather than returning the first in declared list order
    - A schedule with an open-ended band (`effective_to` null) overlapping a dated band raises the same way
    - A schedule with no overlapping bands is unchanged in every respect, including `band_desc` wording, so the two existing schedule tests still pass
    - A production date matching no band still raises the existing no-matching-band `ValueError`
  </behavior>
  <action>
Leave `_select_loanout_rate`'s comparison exactly as it is: `tier.effective_from <=
production_date` and `tier.effective_to is None or production_date <= tier.effective_to`.
Do not convert it to the half-open convention. Converting it would break the committed
Georgia schedule, whose bands abut rather than overlap — under a half-open comparison a
production date of 2025-12-31 would fall past the 2025 band and short of the 2026 band and
match nothing at all, turning a sourced rate lookup into a raise. That is the concrete
reason the convention differs, and it is the reason it is right.

Replace the absence of that reasoning with a comment directly above the comparison stating
three things: that dated "through" ranges are inclusive at both ends by design; that this
is deliberately different from the half-open `low <= x < high` convention
`lookup_flat_rate_by_band` and `engine/net_cash.py::_select_audit_fee_tier` use for numeric
spend and fee bands; and that the two conventions are not interchangeable because a
half-open comparison over abutting dated bands leaves each band's final day unmatched.
Name WR-03 so the next reader finds the decision rather than re-litigating it.

Then add the guard the review asks for. Write a private helper — an overlap check over the
declared bands — and call it from `_select_loanout_rate`'s schedule branch before the
selection loop. Two bands overlap when each one's start is on or before the other's end,
treating a null `effective_to` as unbounded. On overlap, raise `ValueError` naming both
bands' date ranges and both rates, and stating that a schedule covering the same date twice
is a rule-file authoring error rather than something the engine resolves by list order.
Check every pair; do not assume the bands arrive sorted, because the committed Georgia
schedule is declared newest-first and a future rule file may declare them in any order.

Extend `tests/test_engine_credit.py` with
`test_loanout_withholding_schedule_dated_ranges_are_inclusive_at_both_ends`, driving the
committed Georgia-style fixture at 2025-12-31, 2026-01-01 and one band's `effective_from`,
asserting the selected rate for each and asserting in a comment which band each date
resolves to and why. Add
`test_overlapping_loanout_withholding_bands_raise`, building an in-memory
`PerPersonCeiling` whose schedule declares two bands covering a shared date, asserting
`ValueError` and asserting both bands' dates appear in the message. Add a second overlap
case where the open-ended null-`effective_to` band overlaps a dated one.

Do not touch `_record_loanout_withholding`, `_apply_per_person_ceiling`, `_apply_rate` or
any part of `engine/credit.py` that plan 02-07 changed. Do not add a rule-file field.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine_credit.py -q</automated>
    <automated>uv run pytest tests/ -q</automated>
    <automated>uv run ruff check engine/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_engine_credit.py -q` exits 0, including the two pre-existing schedule tests and the three new ones
    - `uv run pytest tests/ -q` exits 0 with the pre-existing 142 tests plus every test added in plans 02-07 and 02-08 still passing
    - `uv run pytest tests/test_engine_credit.py::test_per_person_ceiling_loanout_withholding_selects_earlier_band -q` exits 0 — the pre-existing selection behaviour is unchanged
    - A production date of 2025-12-31 against `synthetic-ga-style.yaml` resolves to the `0.0519` rate and 2026-01-01 resolves to `0.0499`, asserted directly
    - `grep -vE "^\s*#" engine/credit.py | grep -c "production_date <= tier.effective_to"` is 1 — the closed-closed comparison is still there and still singular
    - `grep -n "WR-03" engine/credit.py` returns at least one match in the new comment block
    - A schedule declaring two overlapping bands raises `ValueError` whose message contains both bands' `effective_from` dates, asserted by `test_overlapping_loanout_withholding_bands_raise`
    - `uv run ruff check engine/ tests/` exits 0
  </acceptance_criteria>
  <done>The one lookup in this codebase that reads dated ranges inclusively says so in a comment, proves it at an adjacency point, and refuses an ambiguous schedule instead of picking a rate with no diagnostic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-couple the validation-pairs golden test to price_jurisdiction</name>
  <reversibility rating="reversible">A test-only change; the decoupled direct-path assertions are kept alongside, so reverting means deleting the added ones.</reversibility>
  <precondition>Plans 02-07 and 02-08 are committed, `engine/net_cash.py` implements all four mechanisms including `transferable` (landed by plan 02-04), and `uv run pytest -q` is green</precondition>
  <files>tests/test_engine_against_validation_pairs.py</files>
  <read_first>
    - `tests/test_engine_against_validation_pairs.py` in full — the module docstring recording plan 02-05's deliberate decoupling and its stated reason, the `RULESET_PATH_BY_JURISDICTION` dict, `_gross_credit_for`, the parametrized `test_curated_jurisdiction_reproduces_disclosed_credit`, the two exact-mode headline tests, and the two pair-count non-vacuity guards
    - `engine/pipeline.py::price_jurisdiction` and `price_programme` in full — the `PricedProgramme` shape, how `qualified_spend` becomes a `SpendBreakdown` via `from_total`, how `confidence` is derived from `jurisdiction.status`, and how `source_url` and `date_checked` come from the jurisdiction's first cited source
    - `engine/net_cash.py::convert_to_net_cash` — the four implemented mechanisms and the `NetCashResult` shape, specifically that `transferable` reports a low/high bound with `point` None
    - `jurisdictions/us-ct.yaml` — its `mechanism: transferable` declaration and its `transfer_discount` block, the mechanism the old decoupling existed to avoid
    - `.planning/phases/02-engine-spine-incentive-interpreter/02-04-SUMMARY.md`'s recorded re-coupling recommendation
    - `.planning/phases/02-engine-spine-incentive-interpreter/02-VERIFICATION.md` § "Known, Already-Recorded Items" item 1 — the exact statement of what this weakens and what re-coupling would and would not have caught
    - `jurisdictions/SCOPE-FREEZE.md` RD-03 — the recorded reason the assertion is on gross credit and never on net cash
  </read_first>
  <behavior>
    - Every active validation pair is priced through `price_jurisdiction` and its gross-credit Figure reproduces the disclosed figure, exactly for an exact-mode pair and within the fixture's own declared `tolerance_bps` for a bounded-mode pair
    - Anora reproduces exactly Decimal('991190') through the pipeline path
    - Christmas Always reproduces exactly Decimal('1159502') through the pipeline path, which necessarily runs Connecticut's `transferable` conversion
    - The pipeline-routed gross credit equals the direct base-then-credit gross credit for every pair — the two paths agreeing is the evidence that neither is compensating for the other
    - The existing direct-path assertions still run; nothing is replaced
    - The pair-count guards still hold at three or more New York pairs and one or more Connecticut pairs
    - A mechanism raising `NotImplementedError` fails the test rather than being routed around
  </behavior>
  <action>
Add a `_gross_credit_via_pipeline(pair)` helper beside the existing `_gross_credit_for`. It
resolves the ruleset from `RULESET_PATH_BY_JURISDICTION` the same way, calls
`engine.pipeline.price_jurisdiction` with `Decimal(pair["qualified_spend"])`, selects the
`PricedProgramme` whose `programme_id` equals the fixture's `program_id` — asserting a
match was found, with the same fail-loud message shape `_gross_credit_for` already uses for
a missing programme — and returns that programme's `gross_credit.value`.

Keep `_gross_credit_for` and every assertion that uses it. The point is not to replace the
direct path but to run both and require them to agree: a figure that reproduces through one
path and not the other is a real finding about the composition, which is exactly the seam
CR-01 lived in.

Extend the parametrized `test_curated_jurisdiction_reproduces_disclosed_credit` — or add a
sibling parametrized test over the same `ALL_ACTIVE_PAIRS` list — that asserts the
disclosed figure against the pipeline-routed value using the identical exact-versus-bounded
branching the existing test already implements, and additionally asserts that the
pipeline-routed value equals the direct-path value for that pair. Do not duplicate the
tolerance arithmetic into a second divergent copy; factor the comparison into one helper
both call, so a future tolerance change cannot apply to one path and not the other.

Add `test_anora_reproduces_exactly_through_price_jurisdiction` and
`test_christmas_always_reproduces_exactly_through_price_jurisdiction`, asserting
Decimal('991190') and Decimal('1159502') respectively. For Christmas Always, add one
assertion that the priced programme's `net_cash` carries a low and a high bound with
`point` None, which is what proves the `transferable` conversion actually ran rather than
being skipped — without that assertion the test could pass while the mechanism was never
reached.

Rewrite the module docstring's "Plan 02-05 deviation" paragraph. Do not delete it — replace
it with a short paragraph recording that the decoupling existed because `transferable`
raised `NotImplementedError` before plan 02-04, that plan 02-04 implemented it, and that
plan 02-09 re-coupled the test while keeping the direct path alongside. The history is part
of the audit trail and a silently rewritten docstring loses it.

Assert on the gross-credit Figure only, never on net cash, per RD-03 — routing through
`price_jurisdiction` changes which code path produces the figure, never which figure is
compared against a government disclosure. If a pair stops reproducing through the pipeline
path, report it as a finding with both values; do not adjust a fixture, do not relax an
exact-mode pair to bounded, and do not widen a `tolerance_bps`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine_against_validation_pairs.py -q -v</automated>
    <automated>uv run pytest tests/ -q</automated>
    <automated>uv run ruff check engine/ tests/</automated>
    <automated>bash .github/scripts/vendor-scan.sh</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_engine_against_validation_pairs.py -q` exits 0
    - `uv run pytest tests/test_engine_against_validation_pairs.py::test_anora_reproduces_exactly_through_price_jurisdiction -q` exits 0 — Anora is exactly Decimal('991190') through the pipeline
    - `uv run pytest tests/test_engine_against_validation_pairs.py::test_christmas_always_reproduces_exactly_through_price_jurisdiction -q` exits 0 — Christmas Always is exactly Decimal('1159502') through the pipeline, with `transferable` proven to have run via the low/high/point-None assertion
    - `uv run pytest tests/test_engine_against_validation_pairs.py::test_anora_reproduces_exactly -q` and `::test_christmas_always_reproduces_exactly -q` both still exit 0 — the direct path is kept, not replaced
    - `uv run pytest tests/test_engine_against_validation_pairs.py::test_at_least_three_new_york_pairs_exercised -q` and `::test_at_least_one_connecticut_pair_exercised -q` both exit 0
    - `grep -vE "^\s*#" tests/test_engine_against_validation_pairs.py | grep -c "price_jurisdiction"` is at least 2
    - `git diff --name-only tests/fixtures/validation_pairs/` is empty — no validation-pair fixture was edited
    - `uv run pytest tests/ -q` exits 0 with the pre-existing 142 tests plus every test added across plans 02-07, 02-08 and 02-09 passing
    - `uv run ruff check engine/ tests/` exits 0 and `bash .github/scripts/vendor-scan.sh` exits 0
  </acceptance_criteria>
  <done>The golden-value proof for both curated jurisdictions runs through the engine's real entry point, exercising base to credit to net cash as one composition, with the direct path kept alongside so the two must agree.</done>
</task>

</tasks>

## Artifacts this phase produces

New in this plan — symbols that do not exist before it runs, listed so the source-grounding
pass excludes them from drift verification:

- `engine/credit.py`: a new private overlap-detection helper over
  `loanout_withholding_schedule` bands, called from `_select_loanout_rate`, plus the WR-03
  convention comment block
- `tests/test_engine_credit.py`:
  `test_loanout_withholding_schedule_dated_ranges_are_inclusive_at_both_ends` and
  `test_overlapping_loanout_withholding_bands_raise`
- `tests/test_engine_against_validation_pairs.py`: `_gross_credit_via_pipeline`, a shared
  exact-versus-bounded comparison helper, a parametrized pipeline-routed sweep,
  `test_anora_reproduces_exactly_through_price_jurisdiction` and
  `test_christmas_always_reproduces_exactly_through_price_jurisdiction`

No new files. No new fixture. No rule-file change. Everything else this plan touches
already exists and is listed under the same heading in `02-01-PLAN.md`, `02-04-PLAN.md` and
`02-05-PLAN.md`.

## Source coverage and disposition

This plan implements two of the recommended-scope items dispositioned **INCLUDE** in
`02-07-PLAN.md`'s "Recommended-scope disposition register" — WR-03 and the validation-pairs
re-coupling. That register is the single place every WR/IN item carries an explicit
include-or-defer decision with a reason, including the two **DEFER** decisions on IN-01 and
IN-02 and the deferred negative-base condition; it is not restated here so the two
documents cannot drift.

The edge-probe disposition and the multi-source coverage audit for this gap-closure run
likewise live once, in `02-07-PLAN.md`. The probe rows bearing on this plan's own code path
— INC-02's adjacency question on dated-band boundaries, and INC-01's empty question on a
pair filter matching nothing — are resolved into this plan's `must_haves.truths` above.

**Prohibition recall:** the two-stage prose protocol was run over this plan's three
requirement IDs. Seven kept prohibitions are authored descriptor-less in
`must_haves.prohibitions` above. Canon-referral breadcrumb: the untrusted-input and
deserialisation classes the recall stage surfaced are canon security items owned by the
threat model below, so they were not minted as prohibitions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML rule file → engine | A dated withholding schedule is untrusted structured input; an ambiguous one silently decides a tax rate |
| Caller → engine | `production_date` arrives from outside the engine and selects which band, and therefore which rate, applies |
| Engine → reported figure | The validation-pair test is this project's only claim that a computed figure matches what a government actually paid; if it bypasses the real entry point, the claim is weaker than it reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21 | Tampering | Two overlapping `loanout_withholding_schedule` bands silently resolved by declared list order | high | mitigate | An overlap check runs before selection and raises naming both bands and both rates; every pair is checked because the committed schedule is declared newest-first and order cannot be assumed |
| T-02-22 | Repudiation | An undocumented boundary convention differing from every other lookup in the codebase | medium | mitigate | The closed-closed dated-range convention is stated in a comment naming WR-03 and why it is not interchangeable with the half-open spend-band convention, and proven at the adjacency point by a test over the committed five-band schedule |
| T-02-23 | Spoofing | A golden-value proof that bypasses the code path production actually uses | high | mitigate | Every active pair is re-run through `price_jurisdiction`; the direct path is kept alongside and the two must agree, so neither can compensate for the other. Connecticut's `transferable` conversion is proven to have run by a low/high/point-None assertion rather than assumed |
| T-02-24 | Tampering | A fixture edited to make a re-coupled assertion pass | high | mitigate | `git diff --name-only tests/fixtures/validation_pairs/` must be empty; the prohibition block forbids relaxing an exact-mode pair or widening a `tolerance_bps`, and a non-reproducing figure is reported as a finding |
| T-02-01 | Tampering | Untrusted YAML deserialisation | high | mitigate | `load_ruleset` uses `yaml.safe_load` with a `StrictModel` `extra="forbid"` base — verified by reading the loader this session, not assumed; this plan adds no loader path and no schema field |
| T-02-18 | Elevation of Privilege | A dynamic-resolution escape hatch appearing in the closed handler registry | high | mitigate | No handler is touched; `tests/test_engine_models.py` already asserts dynamic-resolution absence by reading `engine/handlers/` source text, and it runs in this plan's full-suite gate |
| T-02-SC | Tampering | Package-manager installs | high | mitigate | No install occurs in this plan. `02-RESEARCH.md` § "Package Legitimacy Audit" records zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages for this phase; `bash .github/scripts/vendor-scan.sh` is a gate on Task 2 |

ASVS level 1; blocking severity high. This phase parses YAML and computes Decimal
arithmetic — there is no web, auth, session or network surface, and none is invented here.
</threat_model>

<verification>
- `uv run pytest tests/ -q` is green, with the pre-existing 142 tests plus every test added across plans 02-07, 02-08 and 02-09 passing
- Anora reproduces `Decimal('991190')` and Christmas Always reproduces `Decimal('1159502')` through BOTH the direct path and `price_jurisdiction`
- The UK worked example still yields gross `Decimal('7176000')` and net `Decimal('5382000')`
- No file under `tests/fixtures/validation_pairs/` is modified
- `jurisdictions/SCOPE-FREEZE.md`, `jurisdictions/us-ny.yaml` and `jurisdictions/us-ct.yaml` are unmodified
- `uv run ruff check engine/ tests/` exits 0 and `bash .github/scripts/vendor-scan.sh` exits 0
</verification>

<success_criteria>
A jurisdiction's qualifying base computed under its own definition converts through to net
cash by mechanism, net of audit fees, and reproduces the government-disclosed figure
through the engine's real entry point for both curated jurisdictions — and the one lookup
in this codebase that reads dated ranges inclusively records why, proves it at a boundary,
and refuses a schedule that covers the same date twice.
</success_criteria>

<output>
Create `.planning/phases/02-engine-spine-incentive-interpreter/02-09-SUMMARY.md` when done
</output>
