---
phase: 05-curated-breadth-the-validation-loop
plan: 08
type: execute
wave: 3
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - app/services/_paths.py
  - app/services/validate.py
  - app/routers/validate.py
  - app/templates/validate_form.html
  - app/templates/validate_result.html
  - tests/test_engine_against_validation_pairs.py
  - tests/test_app_validate_route.py
autonomous: true
requirements: [JUR-02, JUR-03, JUR-04]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All four curated jurisdictions — New York, California, New Jersey, Connecticut — have a rule file wired into the validation surface, so an anonymous visitor can reproduce a disclosure for any of them (JUR-02, JUR-03, JUR-04, per D-97)"
    - "Every reproduction result carries its disclosure stage, and an allocation-stage or estimated figure is labelled as such wherever it is shown — never presented with the weight of an audited issued credit (JUR-02, JUR-03)"
    - "The cross-jurisdiction sweep in tests/test_engine_against_validation_pairs.py covers all four curated jurisdictions from one generic loop, not four copied blocks (JUR-05)"
    - "A curated jurisdiction with a rule file but no active pair, or an active pair whose jurisdiction has no wired rule file, fails a named test rather than going unnoticed (JUR-02, JUR-03, JUR-04)"
    - "A jurisdiction whose rule file honestly refuses to compute a cash value surfaces that refusal as a readable verdict, never a server error and never an invented number (D-87)"
    - "The pinned golden totals and the live basis-walk honesty gate do not regress"
  artifacts:
    - "app/services/_paths.py — RULESET_PATH_BY_JURISDICTION carrying all four curated jurisdictions"
    - "app/services/validate.py — ValidateResult carrying disclosure_stage"
    - "app/templates/validate_result.html and validate_form.html — stage labelling"
    - "tests/test_engine_against_validation_pairs.py — generic four-jurisdiction sweep plus the acceptance gate"
  key_links:
    - "app/services/_paths.py RULESET_PATH_BY_JURISDICTION -> app/services/validate.py selectable_pairs() -> which pairs an anonymous visitor can run"
    - "fixture disclosure_stage -> ValidateResult.disclosure_stage -> the JSON view and the rendered page"
    - "price_jurisdiction ValueError -> ValidateResult.refusal_reason -> a readable 'cannot be computed' verdict"
---

<objective>
Close JUR-02, JUR-03 and JUR-04 at the surface: wire California, New Jersey and
Connecticut into the validation route alongside New York, carry each pair's
disclosure stage all the way through to what a visitor reads, and generalize the
cross-jurisdiction sweep so a fourth jurisdiction is a data addition rather than
a copied test block.

D-97 restored all three jurisdictions to this phase in full. Plans 05-04, 05-05
and 05-06 built the rule models and their per-jurisdiction proofs; this plan is
where the four become one coherent, inspectable surface.

Purpose: a validated model nobody can run from the hosted URL is a claim in a
test file. ROADMAP Phase 5 success criterion 1 requires that allocation-stage
(California) and estimated (New Jersey) figures are labelled as such "rather
than presented with the same weight as issued figures" — that is a property of
what the page says, not only of what the fixture stores.

Output: four curated jurisdictions live on `/validate`; disclosure stage carried
into both the JSON and HTML views; one generic sweep; one acceptance gate that
fails loudly if any of the four loses coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md

@app/services/_paths.py
@app/services/validate.py
@app/routers/validate.py
@app/templates/validate_result.html
@app/templates/validate_form.html
@tests/test_engine_against_validation_pairs.py
@tests/test_validation_pair_fixtures.py
@tests/test_app_validate_route.py
@tests/test_app_spec_route.py

@.planning/phases/05-curated-breadth-the-validation-loop/05-04-SUMMARY.md
@.planning/phases/05-curated-breadth-the-validation-loop/05-05-SUMMARY.md
@.planning/phases/05-curated-breadth-the-validation-loop/05-06-SUMMARY.md
</context>

<vendor_line>
CI TRIPWIRE — `.github/scripts/vendor-scan.sh` greps every `.py`, `.toml`, `.yaml`
and `.yml` file case-insensitively for the bare tokens
`textract|bedrock|comprehend|rekognition|polly|kendra|sagemaker` and cannot tell a
comment from a call site. A docstring naming one of those services — even to say
the project does not use it — fails CI. Refer to the constraint as "the
permitted-AI-services line" in prose.
</vendor_line>

<scope_boundary>
`data/cost_profiles/us-ca-los-angeles.yaml` declares `jurisdiction_id: null`, a
deliberate, documented Phase 4 decision. Changing it would route California's new
credit into the `/spec` ranking and into the ranked net-cash figures, which is a
change to the cost-comparison product, not to the validation product these three
requirements describe. JUR-02, JUR-03 and JUR-04 each ask for a validated model
against a government disclosure; none of them asks that the model be applied to a
candidate city in the ranker. Leave every file under `data/cost_profiles/`
unchanged in this plan and note the boundary in the summary so the decision is
visible rather than an omission.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire all four curated jurisdictions into the validation surface</name>
  <files>app/services/_paths.py, tests/test_app_validate_route.py</files>
  <behavior>
    - `app/services/_paths.py::RULESET_PATH_BY_JURISDICTION` maps each of the four curated jurisdiction ids to its rule file, and every mapped path exists on disk.
    - `app.services.validate.selectable_pairs()` reports every active pair from all four jurisdictions as selectable.
    - A pair from a jurisdiction with no curated rule file is still returned, marked unselectable, with a readable reason — never silently dropped.
    - `reproduce_disclosure` runs end to end for a pair from each of the four jurisdictions.
    - For a pair whose rule file cannot compute a cash value for want of a sourced input, `reproduce_disclosure` returns a result whose verdict says so and whose refusal reason names the missing input — not an exception escaping to a 500 and not a substituted number.
    - `app.services.spec.RULESET_PATH_BY_JURISDICTION` and `app.services.validate.RULESET_PATH_BY_JURISDICTION` remain the same object, so the two routes can never drift apart.
  </behavior>
  <action>
    Add `us-ca`, `us-nj` and `us-ct` to `RULESET_PATH_BY_JURISDICTION` in `app/services/_paths.py`, each pointing at the rule file under `jurisdictions/` that plans 05-04, 05-05 and 05-06 committed. Replace that dict's comment, which currently says New York is the only wired jurisdiction and names the others as unwired real data — that statement is no longer true and a stale comment beside a security-relevant allowlist is worse than none.

    Read the flow-on effects before writing tests. `app.services.validate.selectable_pairs()` gates selectability on membership of this dict, so every active California, New Jersey and Connecticut pair becomes selectable at `/validate` the moment the dict changes. `reproduce_disclosure` already catches a `ValueError` from `price_jurisdiction` and returns an honest "cannot be computed" result carrying the refusal reason — a guard rail written in Phase 3 that no active pair has ever triggered. Connecticut's transferable programme with no sourced discount range triggers it now, and possibly New Jersey's too, depending on what 05-05 sourced. That is the guard rail doing its job, and this task's test suite must prove it fires for a real committed pair rather than leaving it hypothetical.

    Update `tests/test_app_validate_route.py`: the existing assertions about which pairs are unselectable and why were written when only New York was wired. Correct them to the new truth rather than loosening them — a test that no longer distinguishes a wired jurisdiction from an unwired one has stopped testing anything. Add a per-jurisdiction test that `reproduce_disclosure` completes for one pair from each of the four, and a test that the refusal path returns a readable verdict and a non-empty refusal reason for whichever committed pair now triggers it.

    Do not weaken, bypass or add a switch to the refusal path in `engine/net_cash.py` or `app/services/validate.py`. Do not add a fallback rate. The refusal is the product behaving correctly.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_app_validate_route.py tests/test_app_spec_route.py -q && uv run --frozen python -c "from app.services._paths import RULESET_PATH_BY_JURISDICTION as m; missing=[k for k,v in m.items() if not v.exists()]; assert not missing, missing; print(sorted(m))" && uv run --frozen pytest tests/test_golden_cost.py tests/test_route_a_basis_walk.py -q</automated>
  </verify>
  <done>All four curated jurisdiction ids are mapped to existing rule files; every active pair from those four is selectable; the honest-refusal path is proven to fire for a real committed pair; the spec/validate dict-identity test still passes; the pinned golden totals and the basis-walk gate are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry disclosure stage through to what a visitor reads</name>
  <files>app/services/validate.py, app/routers/validate.py, app/templates/validate_result.html, app/templates/validate_form.html, tests/test_app_validate_route.py</files>
  <behavior>
    - `ValidateResult` carries a `disclosure_stage` field populated from the fixture's own declared stage.
    - The JSON view includes `disclosure_stage` for every result.
    - The rendered result page names the stage for a New York pair, a California pair and a New Jersey pair, and the three read differently.
    - The rendered page explains, for a non-issued stage, what that stage means — that an allocation is an approval to draw against rather than an audited post-production issuance, and that an estimate is a pre-certification figure the same disclosure later adjusts.
    - The pair-selection form shows each pair's stage alongside its title, so a visitor chooses with the stage already visible rather than discovering it afterwards.
    - No result presents a non-issued figure using the wording reserved for an issued credit.
  </behavior>
  <action>
    Add `disclosure_stage: str | None` to `ValidateResult` in `app/services/validate.py` and populate it in `reproduce_disclosure`'s `common_kwargs` from the fixture's own field, so it is present on both the computed and the refused result. Add it to `SelectablePair` too, populated in `selectable_pairs()`, so the form can show it. Follow the existing convention in that module: read with `data.get(...)` and type the field optional, because a malformed fixture missing the key must resolve to a readable absence rather than violating a type hint at runtime.

    Add `disclosure_stage` to `_validate_result_to_json` in `app/routers/validate.py`, in the same flat-string style as the other fields.

    Render it in `app/templates/validate_result.html` beside the production title and again beside the verdict, and in `app/templates/validate_form.html` beside each selectable pair. Write the explanatory wording for the two non-issued stages directly in the template as static prose keyed on the stage value — an allocation is what a commission approved a production to draw against, an estimate is a pre-certification figure that the same disclosure later supersedes with an adjusted certification. Keep the prose short and specific; this is the sentence that stops a reader treating three different kinds of number as one kind.

    Extend `tests/test_app_validate_route.py` with route-level assertions that the rendered page for a pair at each of the three stages names that stage, and that the JSON view carries the field. Assert against the stage values the fixtures actually declare, read from the fixture, not against a hardcoded expectation that could drift from the data.

    Do not add an aggregate that counts pairs across stages, and do not compute an accuracy figure on this surface. Blending stages into one number is the failure ROADMAP Phase 5 success criterion 4 prohibits, and `tests/test_validation_pair_fixtures.py::accuracy_denominator_by_stage` already exists precisely so a caller must ask for stages separately and deliberately.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_app_validate_route.py -q && uv run --frozen python -c "
from app.services.validate import reproduce_disclosure, selectable_pairs
sel=[p for p in selectable_pairs() if p.selectable]
seen={}
for p in sel:
    r=reproduce_disclosure(p.pair_id)
    seen.setdefault(r.disclosure_stage, []).append(r.pair_id)
print(sorted(seen)); assert len(seen)>=3, seen"</automated>
  </verify>
  <done>`ValidateResult` and `SelectablePair` both carry the stage; the JSON view exposes it; the form and the result page both show it; at least three distinct stages are reachable across the selectable pairs; the route tests assert the stage read from the fixture rather than a hardcoded string.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generalize the cross-jurisdiction sweep and add the four-jurisdiction acceptance gate</name>
  <files>tests/test_engine_against_validation_pairs.py</files>
  <behavior>
    - The module's jurisdiction-to-rule-file map carries all four curated jurisdictions.
    - The active-pair sweep is built by one loop over that map, not by four hand-maintained per-jurisdiction lists.
    - Every active pair from every curated jurisdiction is asserted against its disclosed figure through the direct base-then-credit path, under its own declared assertion mode.
    - Every pipeline-routable active pair is additionally asserted through `price_jurisdiction`, and the two paths must agree — a pair whose rule file cannot complete is excluded structurally by reading the loaded programme, never by a jurisdiction-id skip.
    - A curated jurisdiction with a rule file but zero active pairs fails a named test that says which jurisdiction.
    - A jurisdiction id declared by an active fixture but absent from `app/services/_paths.py`'s map fails a named test that says which id.
    - The existing named single-production tests continue to pass unmodified in substance.
  </behavior>
  <action>
    Widen `RULESET_PATH_BY_JURISDICTION` in `tests/test_engine_against_validation_pairs.py` to all four curated jurisdictions, then replace the hand-rolled `NY_ACTIVE_PAIRS`/`CT_ACTIVE_PAIRS` pattern with a single dict comprehension over that map, keeping `ALL_ACTIVE_PAIRS` as the flattened sweep input. Keep every existing fail-loud guard: the empty-glob `RuntimeError`, the empty pipeline-routable `RuntimeError`, and the per-jurisdiction minimum-count tests — and add the missing two so the guarantee is uniform across the four rather than accidental. Keep `_pipeline_can_complete` structural, reading the loaded programme's mechanism and discount bounds, so a future rule-file update that sources a real range is picked up automatically instead of staying silently excluded.

    Keep `test_anora_reproduces_exactly`, `test_christmas_always_reproduces_exactly` and both pipeline-routed variants exactly as they are in substance. They are named, independent anchors on real government figures and the generalization must not absorb them into the parametrized sweep — an anchor that only exists inside a loop over data can be silently emptied by a data change.

    Add the acceptance gate as two tests. The first asserts that every jurisdiction id in this module's map has at least one active validation pair, naming any that does not. The second imports `app.services._paths.RULESET_PATH_BY_JURISDICTION` and asserts that every jurisdiction id declared by an active fixture appears in it, naming any that does not — this is what makes a jurisdiction that has a rule file and pairs but is not reachable from the hosted surface a test failure rather than an invisible gap.

    Update the module docstring: its current text describes a New York-and-Connecticut world and explains the jurisdiction map as a "one-line addition" pattern. Rewrite it to describe the four-jurisdiction sweep, keep the plan 02-09 finding about the transferable refusal intact because it is still live and still true, and record that the per-jurisdiction proofs live in the three dedicated test modules plans 05-04, 05-05 and 05-06 created.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_engine_against_validation_pairs.py tests/test_jurisdiction_us_ca.py tests/test_jurisdiction_us_nj.py tests/test_jurisdiction_us_ct.py tests/test_validation_pair_fixtures.py -q && uv run --frozen pytest -q && bash .github/scripts/vendor-scan.sh && bash .github/scripts/lockfile-scan.sh</automated>
  </verify>
  <done>The sweep covers all four curated jurisdictions from one loop; both acceptance-gate tests exist and pass; the four named single-production anchors are unchanged in substance; the full suite is green and both CI scan scripts exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor -> /validate | An untrusted `pair_id` string selects which fixture and rule file are read |
| fixture data -> rendered page | A stored disclosure stage becomes the claim a reader takes away about what a number means |
| rule file -> published cash figure | A rule file that cannot complete must refuse visibly rather than fail open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-33 | Elevation of privilege | `reproduce_disclosure` path construction | critical | mitigate | the closed-allowlist check on `pair_id` remains the first statement of the function body and the widened jurisdiction map only widens which committed fixtures are members — no untrusted string reaches an `open()` call; assert this explicitly in the route tests |
| T-05-34 | Repudiation | disclosure-stage labelling | high | mitigate | the stage is carried on `ValidateResult` and `SelectablePair` from the fixture's own field and rendered on both the form and the result page, so an allocation or an estimate cannot be read as an audited issuance |
| T-05-35 | Denial of service | rule file that cannot complete | high | mitigate | `price_jurisdiction`'s `ValueError` is caught and returned as a readable refusal with its reason; proven to fire for a real committed pair rather than remaining an untriggered guard |
| T-05-36 | Tampering | jurisdiction allowlist drift | medium | mitigate | `app.services.spec` and `app.services.validate` share one dict object and the identity is asserted; the new acceptance gate fails if an active fixture's jurisdiction is missing from it |
| T-05-SC | Tampering | package-manager installs | high | accept | this plan installs no package; `pyproject.toml` and `uv.lock` are not in `files_modified` and `.github/scripts/lockfile-scan.sh` remains the standing gate, re-run in Task 3 |
</threat_model>

<verification>
- `uv run --frozen pytest -q` — full suite green, no pre-existing test removed, skipped or loosened.
- `bash .github/scripts/vendor-scan.sh` and `bash .github/scripts/lockfile-scan.sh` — both exit 0.
- `git diff --stat -- engine/ data/cost_profiles/` — empty.
- `uv run --frozen pytest tests/test_golden_cost.py tests/test_route_a_basis_walk.py -q` — the pinned golden totals (NY $758,427 / LA $693,521 / London £548,595 / gap $64,906) and the live honesty gate do not regress.
- `uv run --frozen python -c "import app.main"` — the app imports with no SDK loaded and no key present.
</verification>

<success_criteria>
- Four curated jurisdictions are reachable from `/validate` for an anonymous visitor.
- Every result, in both views, carries and displays its disclosure stage, with non-issued stages explained.
- One generic sweep covers all four; losing coverage for any of them fails a named test.
- A jurisdiction that cannot compute a cash value refuses visibly and no number is invented.
</success_criteria>

<output>
Create `.planning/phases/05-curated-breadth-the-validation-loop/05-08-SUMMARY.md` when done.
Record in it: which committed pairs now trigger the honest-refusal path and why, the stage wording chosen for each non-issued stage, and the cost-profile scope boundary this plan deliberately did not cross.
</output>
